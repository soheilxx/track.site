# 15 - Living AI Core

The ambient layer of the Track AI panel (owner supplement §9 "Living AI Core", "Technische Progressive-Enhancement-Architektur", "Kontrolle, Barrierefreiheit und Lesbarkeit", "Verbindliche Abnahmekriterien"). Code: `apps/web/src/components/app/shell/living-ai-core/**`, styles: the `.lac-*` block in `apps/web/src/app/globals.css`, wiring: `assistant-panel.tsx` (ambient slot + header control).

The layer is decorative. It only conveys **that** Track AI waits, listens, works, streams, needs an approval, finished or is blocked; the localized activity sentences, cards and error texts remain the authoritative status. Nothing in it is derived from chain-of-thought, reasoning tokens, token counts or invented progress, and it never produces analytics, fingerprinting data or requests.

## 1. Architecture

| Piece | File | Responsibility |
| --- | --- | --- |
| `LivingAICore` | `living-ai-core.tsx` | The swappable component. Props are exactly `{ state, motion, mode, now? }`. Renders the static markup on the server, resolves the tier on the client, lazily loads the WebGL renderer, owns the frame loop and every observer/listener, releases everything on unmount. No React state per frame. |
| State machine | `state-machine.ts` | Pure, clock-injected: priority resolution, 400–700 ms interpolation, one-shot success wave, integrated breathing phase, the state → parameter table. No hold of its own — hysteresis belongs to the state source (below). |
| Shapes | `blobs.ts` | Pure layout of the 5 shapes (position, radius, colour) from a machine sample: rise, drift, merge (working), lean (listening), contract (blocked). |
| Tier logic | `tier.ts` | `effectiveMotion`, `selectTier`, `isConstrainedDevice` / `webglPermitted` (device rule of §2), the frame-budget monitor, frame interval and render scale. |
| Upgrade gate | `upgrade-gate.ts` | Pure, primitive-injected timing gate of the WebGL upgrade (§2): document loaded → ≥ 3 s since navigation start on the injected clock → idle callback with timeout; `browserUpgradeEnv` binds the DOM; the returned cancel releases the load listener, timer or idle callback. |
| Renderer | `webgl-renderer.ts` | WebGL2 metaball fragment shader, full-screen triangle, uniforms only; `null` when WebGL2 is unavailable or a major performance caveat exists; handles `webglcontextlost`; `dispose()` releases program, shaders, VAO and the context. Loaded with `import()` once the upgrade gate opens. |
| Preferences | `preference.ts` | External stores (`useSyncExternalStore`) for `<html data-ai-motion>` (per-user setting) and `prefers-reduced-motion`; `useHydrated`; coarse-pointer detection and `readDeviceHints` (pointer, data saver, device memory, cores) for the device rule. |
| State source | `assistant-ambient.tsx` → `useAssistantUiState()` (`components/chat/assistant-ui-state.ts`) | The panel has **one** motion state source. `useAssistantUiState` derives the state from the assistant store's `ChatState` (status `idle` / `sending` / `working` / `streaming` / `reconnecting`, approval, error, the activities of the turn with their latest phase, the verified outcome, draft, composer focus) with the fixed priority, a 500 ms minimum hold per state (`UiStateDebouncer`, latest target after the hold wins) and the 900 ms success hold, all on an injectable clock. `AssistantHost` writes the same value to `data-ai-state` on the panel container; `AssistantAmbient` (the panel's default ambient content) passes it to `<LivingAICore state>` together with the user's motion preference — so `data-ai-state` and the core's `data-state` are always the same value and an e2e/visual test may bind to either. |
| Control | `motion-control.tsx` | Header button "Pause AI motion" / "Turn AI motion on" (state carried by the accessible name and icon, deliberately no `aria-pressed` alongside a changing label). Writes `off` / `system` optimistically to `data-ai-motion` and persists it through `updateAiMotionAction` (audited); a failed save reverts and is announced in a live region. Labels: `shell.assistant.motion.*` ×6. |

The panel's ambient slot is `absolute inset-0 -z-10 overflow-hidden`, `aria-hidden`, `pointer-events-none`. The core itself is `position: absolute; inset: 0; contain: strict; isolation: isolate; transform: translateZ(0)`; the animated region (`.lac-core`) is the top 14 rem (24 rem in `onboarding` mode) with a static mask that fades to transparent towards the message area, plus a thin static edge accent over the full panel height. Changing state or tier only changes data attributes, custom properties, `transform` and `opacity` — never width, height, margins, padding, scroll position, focus or hit areas. The unit test `living-ai-core.dom.test.tsx` ("geometry invariants") checks that the element tree, class names and inline styles are identical across all states and tiers.

## 2. Tiers

| # | Tier | When | What renders |
| --- | --- | --- | --- |
| 1 | `static` | SSR / before hydration; `prefers-reduced-motion: reduce` with setting `system`; settings `reduced` and `off` | `.lac-base`: three radial gradients from the theme tokens (`--color-primary-soft`, `--color-violet-soft`, `--color-cyan-soft`), so light and dark are correct before any script runs. States are static colour accents (`.lac-edge`, `.lac-halo`, base opacity). `reduced` keeps 600 ms opacity cross-fades between states; `off` disables every transition (`[data-pref="off"]`). No keyframes, no `requestAnimationFrame`, no canvas. |
| 2 | `css` | Hydrated and motion allowed — always the first animated tier; stays while the upgrade gate is closed or WebGL is loading, unavailable, failed, lost or downgraded; the final tier on constrained / mobile-class devices unless the setting is `full` | Three radial-gradient shapes (`.lac-blob`) whose inner element runs `lac-drift-a/b/c` keyframes (16 / 17.5 / 14.5 s) and whose wrapper takes state offsets — transform and opacity only. Approval and blocked pause the keyframes; success runs the one-shot `lac-wave` (800 ms). |
| 3 | `webgl` | Hydrated, motion allowed, device not constrained (or setting `full`), the upgrade gate open (document loaded, ≥ 3 s since navigation start, main thread idle), `createWebglRenderer` succeeded | 5 metaballs in cobalt, violet and cyan (light/dark variants) with breathing, rise, merge, lean, contract, halo, outline, edge and the success ring, drawn from the machine sample every frame. The canvas cross-fades in over the CSS shapes (`.lac-gl` / `.lac-css` opacity transitions, `--lac-t` 600 ms) and out again on a downgrade. |

Motion setting → effective behaviour (`effectiveMotion`): `off` → static, `reduced` → static, `system` → follows the OS, `full` → animated (explicit user choice, even under an OS `reduce`). The elements carry `data-motion="essential"` so the global reduced-motion rule of the design system does not override the component's own resolution.

Fallbacks are silent: `selectTier` returns `css` whenever WebGL is `unknown`, `loading`, `unavailable`, `failed` or the frame budget was missed persistently; the chat is never affected.

### Tier selection: timing and device rule (docs/16 D17)

The CSS tier renders first, always. WebGL is an *upgrade* that `LivingAICore` requests through `upgrade-gate.ts` only when every one of the following holds, checked in this order:

1. **Page fully loaded** — `document.readyState === "complete"`, otherwise the gate waits for the `load` event.
2. **≥ 3 s since navigation start** — `UPGRADE_MIN_ELAPSED_MS` on the component's clock (`performance.now()` by default, which counts from the navigation; a test-injected `now` must use the same time base). A timer bridges the remainder and re-reads the clock when it fires.
3. **Main thread idle** — `requestIdleCallback` with a 4 s timeout (`UPGRADE_IDLE_TIMEOUT_MS`); where the API does not exist (WebKit) a 500 ms timer stands in.

Only then does the renderer chunk load (`import()`), compile its shaders and draw — never inside the first-paint / load window, so the enhanced tier cannot add main-thread work to LCP or TBT of the mobile Lighthouse run. The upgrade has no visible jump: the first frame is drawn while the canvas is still fully transparent, the tier attribute flips one animation frame later (so the browser holds a computed style for the canvas), and `.lac-gl` fades in while `.lac-css` fades out over `--lac-t` = 600 ms (globals.css; the required 400–700 ms window). A downgrade runs the same cross-fade in reverse before the context is released.

**Device rule.** `readDeviceHints` (feature detection only, no user-agent sniffing) classifies a device as constrained / mobile-class when `(pointer: coarse)` matches, `navigator.connection.saveData` is on, `navigator.deviceMemory ≤ 4` or `navigator.hardwareConcurrency ≤ 4`; unknown values never count. On such a device the `css` tier is final for `system` — the gate is not even armed — and WebGL is attempted only when the user chose `full` explicitly (`webglPermitted`, `selectTier({ constrained })`). The device hints are read once per mount.

**Preference changes.** Turning the engine off by preference (`off`, `reduced`, or `full` → `system` on a constrained device) releases the renderer through the engine cleanup and forgets its status, so a later change back goes through the gate and the cross-fade again instead of flipping the tier the moment the canvas mounts. Failures (`unavailable`, `failed`, a persistently missed frame budget) are never retried within a mount. Everything else — states, reduced motion, `off`, pausing, context loss, cleanup — is unchanged by the gate.

## 3. State table

Priority when facts compete: `blocked > approval_required > working > streaming > success > listening > idle` (`CORE_STATE_PRIORITY`).

| State | Source (real UI/backend fact) | Parameters (`STATE_PARAMS`) | Visible behaviour |
| --- | --- | --- | --- |
| `idle` | no active turn, no approval | period 16 s, amplitude 0.35, speed 1 | very slow breathing (14–18 s per cycle), low amplitude, calm open shapes |
| `listening` | focus in the composer or a started draft | lean 1, halo 0.7 | one shape leans towards the composer, subtle cyan halo; no per-keystroke change |
| `working` | chat status `sending` (request in flight), `working` (`activity.started`, `job.progress`) or `reconnecting` (the same turn is resumed) | period 11 s, speed 1.6, merge 1, glow 0.35 | two to three shapes merge into a calm directed flow towards the activity line; more energy, no fake progress |
| `streaming` | released assistant output being transferred | speed 1.1, merge 0.3, glow 0.6 | minimal light flow at the core; never a change per token (repeated identical requests are no-ops) |
| `approval_required` | pending approval card (`ui.approval` / `approval.required`) | speed 0.06, amplitude 0.1, outline 1, contract 0.1 | motion almost stops; calm amber outline; no blinking |
| `success` | verified success outcome (`ui.final` without error, also from a confirmed action) while the turn is at rest (`idle`), no error, no failed tool run; held 900 ms from the outcome's timestamp by the state source | wave 0 → 1 over 800 ms | one restrained emerald expansion wave, then a soft return to idle |
| `blocked` | chat error (`error` event, failed request, lost stream) / a tool run of the turn that ended in `activity.failed` or `activity.blocked` / a verified blocked outcome of the final answer. A block that only asks for a confirmation (`CONFIRMATION_REQUIRED`) belongs to `approval_required`, never here | speed 0.12, amplitude 0.08, contract 0.7, edge 1 | shapes contract and become almost static; muted amber/red edge; never shaking or strobing |

Hysteresis lives in the state source only: `useAssistantUiState` holds every state for at least 500 ms and applies the latest target once the hold has elapsed, so bursts of backend events never flicker and per-keystroke store updates (an unchanged target) never restart anything. The machine adds no second hold — a requested state is committed at the next sample (frame) — which keeps the host's `data-ai-state` and the core in step. After a commit every parameter interpolates from its *current* value to the target over 550 ms with an ease-in-out curve (window 400–700 ms); an interrupted transition continues from where it is. `success` is a one-shot that always completes: a state requested while the 800 ms wave runs is committed on the sample that ends the wave (the source may release `success` after its 500 ms minimum hold), and on the CSS tier `.lac-wave` retracts through a transition instead of popping away when the state leaves early. The breathing phase is integrated with the state's speed, so slowing down never snaps a shape.

## 4. Performance budget

- Frame rate: ≤ 30 fps (`frameInterval`), ~22 fps on coarse-pointer devices. Frames are skipped by comparing the injected clock, never by frame counting.
- Resolution: canvas backing store = CSS size × min(DPR, 1.5) (1.25 on coarse pointers), upscaled by the browser.
- Work per frame: one `drawArrays` of a full-screen triangle, 5 shapes, 16 uniform writes into pre-allocated `Float32Array`s. No allocations, no React state, no DOM writes.
- Frame budget: `createFrameBudget` ignores 12 warm-up frames, then keeps a window of 30 rendered frames; when 40 % of the intervals exceed 2.5 × the target interval the renderer stops, the canvas fades out (700 ms) and the context is released — the CSS tier stays for the rest of the mount.
- Pausing: Page Visibility (`visibilitychange`), `IntersectionObserver` on the root (off-screen panel), unmount (minimised panel, closed drawer/sheet). A paused renderer produces no frames at all.
- Failure paths: `getContext` exceptions, `failIfMajorPerformanceCaveat`, shader compile/link errors → `null` → CSS; `webglcontextlost` → `failed` → CSS; import failure → CSS.
- Load-time cost: the WebGL upgrade never runs inside the load window (§2 tier selection: document loaded, ≥ 3 s since navigation start, idle callback); constrained / mobile-class devices stay on the CSS tier unless the setting is `full`.
- Cleanup on unmount: the pending `requestAnimationFrame` (frame loop and the deferred tier flip), the upgrade gate's load listener / timer / `requestIdleCallback`, the `IntersectionObserver`, the `ResizeObserver`, the `visibilitychange` and `webglcontextlost` listeners, program, shaders, VAO and the context (`WEBGL_lose_context`).
- Not done on purpose: no `will-change` by default, no full-viewport blur, no SVG goo filters, no animated `backdrop-filter`, no renderer/GPU queries.

Acceptance evidence (supplement §9 "Verbindliche Abnahmekriterien"), status after the two follow-up runs of 2026-09-05 (`docs/16-release-report.md` §6.6, §9, §11.2). Rows marked "follow-up 2" were measured on the rebuilt production build `9ZJAGzR3pMhmuGkmeGTk8` (task FX fixes incl. the tier selection rule of §2) served on port 3016 with the stored owner session (`docs/qa/2026-09-05/followup2/living-core/`, `README.md` indexes every artifact; `summary.md` is generated by `living-core-report.mjs` from the JSON files and ends with the hand-written `summary-notes.md`); the other rows on the first follow-up's build `JkuZkqiqEn0HgN4FIDCyT`; chromium headless shell with `--use-angle=d3d11` unless stated:

| Criterion | Status | Evidence |
| --- | --- | --- |
| Reduced motion / setting `off` / hidden tab: no continuous animation frames | **pass** (first follow-up, re-confirmed in follow-up 2 on the rebuilt bundle) — 0 `requestAnimationFrame` callbacks in 10 s windows under `prefers-reduced-motion: reduce` (10 017 ms), after the header toggle to `off` on a page that was on the `webgl` tier (`static` 75 ms after the click, 10 013 ms window) and after a reload with the persisted `off` (10 004 ms); `data-tier="static"` in the SSR HTML, no canvas, no running animation inside `.lac`; the WebGL2 context was released on the toggle (`webglcontextlost` 1 of 1 created); preference restored to `system`, after which the gate re-armed and the tier returned to `webgl`. Hidden tab: unit test `living-ai-core.dom.test.tsx` (visibility pause) — not measured in a browser | follow-up 2: `docs/qa/2026-09-05/followup2/living-core/motion-off.json`, `motion.log`, `summary-notes.md` §(d); first: `docs/qa/2026-09-05/followup/living-core/motion-off.json`, `motion.log` |
| Lighthouse median (3 mobile runs) at most 3 points below the static chat version | **FAIL — 6 points** before the fix: panel docked (mobile preset, screen widened to 1280 × 800 @ DPR 2 so the core mounts on the `webgl` tier, verified in `lighthouse/tier-checks.json`): `off` 75 / 79 / 77 → median 77; `system` 70 / 71 / 71 → 71; `full` 76 / 71 / 70 → 71; TBT +209 / +198 ms, LCP +81 / +83 ms, CLS +0.001; control on the default phone emulation (panel closed, core not mounted) 83 vs 83. `full` / phone group missing (runner stopped). Defect D17 in docs/16 §9. **Fix applied 2026-09-05 (task FX)**: the tier selection rule of §2 (CSS first; WebGL only after load + 3 s + idle; constrained devices stay on CSS for `system`) — unit-tested (`upgrade-gate.test.ts`, `living-ai-core.dom.test.tsx`). **Re-measured 2026-09-05 (follow-up 2, task MS): pass, −1 point** — same docked method, 3 runs each on build `9ZJAGzR3pMhmuGkmeGTk8`: `off` (tier `static`) 82 / 79 / 81 → median **81**; `system` (tier `css` — the mobile emulation's coarse pointer makes the device rule keep `system` on CSS, no gate armed) 80 / 81 / 80 → **80**; `full` (tier `webgl`, flip observed 3 105 ms after the load event) 80 / 80 / 80 → **80**. Deltas animated − static: **−1 / −1 points**, TBT −8 / −12 ms (107 → 99 / 95 ms), CLS 0.000 (0.008 all), main-thread work 1.45 → 1.41 / 1.41 s, script boot-up 675 → 664 / 638 ms; the load-window penalty of the renderer chunk is gone. Simulated LCP +315 / +320 ms (4.69 → 5.00 / 5.01 s) with the same LCP element in all 9 runs, identical FCP (1.67–1.70 s) and observed TTFB + render delay not higher for the animated groups (`lcp-elements.json`) — an open observation on the Lantern value, not a failure. The static baseline itself moved between builds (77 → 81, D9 PERF changes), so only the within-run deltas (−6 → −1) compare | follow-up 2: `docs/qa/2026-09-05/followup2/living-core/lighthouse/summary.md`, `summary.json`, `tier-checks.json`, `lcp-elements.json`, `app--{off,system,full}--docked--run{1,2,3}.report.json`, `run.log`; before: `docs/qa/2026-09-05/followup/living-core/lighthouse/summary.md`, `summary.json`, `app--{off,system,full}--docked--run{1,2,3}.report.json`, `app--{off,system}--phone--run{1,2,3}.report.json`, `run.log`; runner `apps/web/scripts/qa/living-core-lighthouse.mjs`; fix gates in `docs/qa/2026-09-05/followup2/fx/` |
| Idle animation causes no attributable long tasks > 50 ms; scrolling, typing and buttons stay smooth | **pass on this machine (follow-up 2)** — `living-core-budget.mjs --mode longtasks --idle-s 120 --scroll-s 60 --type-s 60`, desktop viewport 1440 × 900 (fine pointer, so `system` upgrades), tier `webgl` confirmed via `data-tier` before the window (read 3 111 ms after navigation) and unchanged through the end: **0** `longtask` entries in 120.0 s idle (7 201 rAF callbacks, p50 0.1 / p95 0.3 / max 0.4 ms, 0 > 16 ms), 60.1 s scrolling (253 wheel events, 0 long tasks) and 60.6 s typing (1 280 characters, 0 long tasks, rAF max 0.6 ms). Chrome trace over the same 241 s: 158 971 main-thread tasks, none ≥ 32 ms, longest 18.5 ms (`focusin` on the composer click), the six 16–32 ms tasks all input handling in the typing phase; the animation's own cost `FireAnimationFrame` mean 0.22 ms. Real reference devices remain owed (docs/16 D16 device lab) | `docs/qa/2026-09-05/followup2/living-core/longtasks-d3d11.json`, `longtasks-d3d11.trace-summary.json`, `longtasks.log`, `summary-notes.md` §(a) (the 341 MB trace itself is outside the pack, path in the JSON) |
| ≥ 30-minute run with state changes: no growth of memory, listeners, canvases, WebGL resources | **pass — growth bounded, tracks the transcript (follow-up 2)** — `--mode soak --soak-min 30 --sample-s 60 --step-s 20`: 30.0 min, 89 steps / 44 stubbed turns at the `fetch` boundary (29 success streams, 15 failed runs), every state except `approval_required` entered, tier `webgl` in every sample. GC'd samples: JS heap 7.58 MB (minute 5, 37 messages) → 9.21 MB (minute 30, 99 messages) = **+1.63 MB for +62 transcript messages** with a falling per-message increment (58 → 16 → 19 KB); Chrome `JSEventListeners` 380 → 390 → 390 → 390 → 391, the in-page listener ledger net **327 constant** from minute 5 (8 069 added / 7 742 removed — every registration of the 44 turns removed again; the only per-type +1s are Playwright's own actionability listeners); exactly **1 canvas / 1 WebGL2 context created / 0 lost** throughout, IntersectionObserver 2 / ResizeObserver 2 / MutationObserver 3 → 4 constant; 108 446 rAF callbacks (60.2 / s, p95 0.3 ms). A session that never adds messages was not measured separately, so "zero growth" is not claimed. One 83 ms long task at 3.01 s after navigation = the WebGL upgrade (renderer import + shader compile + first frame), before the first sample and outside every idle window — observation, not a failure | `docs/qa/2026-09-05/followup2/living-core/soak.json`, `soak-samples.csv`, `soak.log`, `soak-stdout.log`, `summary-notes.md` §(b) |
| Disabled / unavailable / lost WebGL context → CSS or static fallback, chat stays usable | **pass in Chromium 151 and WebKit 26.5**: WebGL2 → `webgl` (canvas opacity 1, ≤ 30 fps: Chromium 54 draws / 2 s, WebKit 37 draws / 2 s; backing store capped at 1.5 × on WebKit's DPR 2 → 599 × 336 px for 399 × 224 CSS px); reduced motion → `static`; `getContext("webgl2")` = `null` → `css` with `lac-drift-a/b/c` running and the composer typable; `WEBGL_lose_context` → `css`, canvas released; dark theme → same tier; WebKit has no `requestIdleCallback` (the `setTimeout` fallback runs). Not demonstrable here: the software-only → CSS fallback, because the headless shell accepts SwiftShader as a non-caveat WebGL2 context | `docs/qa/2026-09-05/followup/browsers/runs/xb-lac-{chromium,webkit}/lac/*.json` + `*.webp`, `browsers/summary.md` §4; `living-core/webgl-probe.json` |
| Panel keeps its size in every state; only the message area scrolls; CLS = 0, no scroll-position change, composer and last message visible with the keyboard open | **pass after the E3 fix**: the transcript kept its `scrollTop` while its height shrank with the emulated keyboard (375 × 812 → 375 × 430) and hid the last message; `AssistantMessages` now re-pins the end of the list through a `ResizeObserver` (`apps/web/src/components/chat/assistant-chat.tsx`). After: last message 249–325 px inside the list, composer 354–418 px focused, no idle layout shift with the core animating. Real device keyboard still owed (headless Chromium cannot shrink the visual viewport alone) | `docs/qa/2026-09-05/followup/keyboard/README.md`, `after-fix/keyboard-metrics.json`, e2e `app.spec.ts` "mobile on-screen keyboard" |
| Chat, setup state, jobs and scroll position survive route changes; site switch confirms the context | **pass** for transcript, scroll position, draft and panel geometry and for the site switch (context line + header announcement, `data-total` 0 for the other site, 250 restored); a turn in flight (job state) not exercised — no provider in the lab | `docs/qa/2026-09-05/followup/state/README.md`, `after-fix/route-change.json`, `after-fix/site-switch.json`, e2e `app.spec.ts` "Track AI state across routes and sites" |
| Text and controls WCAG 2.2 AA in light and dark mode | pass: the contrast review of every axe "incomplete" node (1 935 evaluations, 5 root causes fixed — the stage accent tokens and the dark / stage `--color-primary-soft`, which also tints the static tier) and 204 / 204 dashboard nodes in the dark theme | `docs/qa/2026-09-05/followup/contrast/README.md`, `results-after.json`, `results-dark.json` |
| Safari/iOS, Chrome/Android, Safari, Chrome, Firefox, Edge; light / dark, long chats, 200 % zoom, narrow viewports | **Chromium and WebKit only** (desktop + Playwright emulation "iPhone 14" / "Pixel 7": bottom sheet viewport-high, composer inside the viewport, no horizontal scroll, axe 0). Firefox cannot start on this machine (docs/16 D19); WebKit needed a header-strip hook for local builds (D18) and logged one viewport-meta console message on `/app` (D21) — both fixed in code in follow-up 2 (CSP directive gated on the `HOST_MARKETING` scheme; `interactive-widget` appended at runtime on Chromium only) but **not yet re-run in WebKit**; Edge, real devices and 200 % zoom not run | `docs/qa/2026-09-05/followup/browsers/summary.md` §0, §5, §6; `docs/qa/2026-09-05/followup2/fx/README.md` |
| Visual comparison ("ruhig, lebendig, hochwertig") | **not run** (no user study in any task) | — |

Every check that could run is reproducible with the scripts under `apps/web/scripts/qa/living-core-*.mjs` (`living-core-budget.mjs` modes `longtasks`, `soak`, `motion`, `tier`; `living-core-lighthouse.mjs`; `living-core-report.mjs` regenerates `living-core/summary.md`; `living-core-webgl-probe.mjs`).

## 5. Accessibility and control

- The core and everything inside it are `aria-hidden`, `pointer-events: none` and outside the tab order; the chat stays plain HTML.
- Text and controls keep WCAG 2.2 AA in both themes: the light tier alpha is ≤ 0.2 of the accent colours over `surface`, the dark tier ≤ 0.28, and the mask fades the layer out before the message area.
- `prefers-reduced-motion` is honoured without a flash: SSR paints the static tier, the client only enables keyframes after the effective motion resolved to `animated`; `reduced` and `off` never run keyframes or frames.
- The per-user setting (`workspace_preferences.ai_motion`, Settings → AI motion) has four values; the panel header adds a directly reachable pause / turn-on toggle that writes the same preference through the same audited action.
- No blinking, strobing, shaking, fast brightness changes or large zoom/parallax anywhere in the state table.

## 6. Testing with injected time

Every time-dependent piece takes a clock:

```ts
const clock = { t: 0, now: () => clock.t };
const machine = createCoreStateMachine({ now: clock.now });
machine.request("working");
const a = machine.sample();  // committed at once (no hold of its own): progress 0, params = idle
clock.t = 550;               // 550 ms later
const b = machine.sample();  // progress 1, params = working
```

`<LivingAICore now={clock.now} …/>` uses the same clock for frame pacing, the frame budget and the machine; tests drive `requestAnimationFrame` manually, so the picture for a given `(state, t)` is deterministic — the basis for visual-regression snapshots.

The state source takes a clock of the same kind: `useAssistantUiState({ clock, minHoldMs, successHoldMs })` compares `clock.now()` with the store's `outcome.at` (a `Date.now()` timestamp) and schedules the hold and the success expiry through `clock.setTimeout`, so both are exact in tests (`resolveUiState`, `inputsFromChat` and `UiStateDebouncer` are the pure parts, tested in `components/chat/assistant-ui-state.test.ts`). Tests of the wired panel run on Vitest's fake timers and advance them by `DEFAULT_MIN_HOLD_MS` after every store change before reading the core's `data-state`.

| Test file | Covers |
| --- | --- |
| `state-machine.test.ts` | priority for every pair, commit on the next sample without a hold of its own, withdrawn requests, 400–700 ms transitions, interrupted transitions, no restart on repeated requests, success one-shot that completes before a state requested meanwhile applies, breathing slow-down, determinism |
| `tier.test.ts` | effective motion, tier matrix (no WebGL → CSS, reduced/off → static, downgrade → CSS, constrained device → CSS unless `full`), `isConstrainedDevice` / `webglPermitted`, frame budget, frame interval, render scale |
| `upgrade-gate.test.ts` | the timing gate with faked primitives: load → 3 s mark → idle in that order, the clock re-check when the timer fires, custom thresholds, cancel releases the load listener / timer / idle callback, start exactly once |
| `blobs.test.ts` | determinism, bounds, merge/lean/contract effects |
| `living-ai-core.ssr.test.tsx` | SSR output is deterministic, static, token-based, without canvas/script/inline geometry |
| `living-ai-core.dom.test.tsx` | tier selection in a DOM (no WebGL → CSS, reduced → static, WebGL → frames at ≤ 30 fps, budget miss → downgrade, context loss → CSS, hidden tab pause), the upgrade gate on the injected clock (no request before load / 3 s / idle, timer re-check, first frame drawn before the deferred tier flip), constrained devices (`system` stays on CSS, `full` upgrades, `full` → `system` releases and re-gates), lifecycle cleanup incl. the gate and the deferred flip, geometry invariants |
| `assistant-panel.test.tsx` | ambient slot attributes, default ambient inside the provider driven through the store's public API on a fake clock (the single 500 ms hold, tool runs → working, confirmation + approval → approval_required, failed run → blocked, draft/focus → listening without restarts on typing, `ui.final` → success for the hold, then idle), header control (labels, no `aria-pressed`, optimistic write, persist, revert on failure) |
| `components/chat/assistant-ui-state.test.ts` (other slice) | the source itself: priority, `inputsFromChat` (store vocabulary → facts, confirmation blocks vs. failures, success only at rest), the debouncer's hold and determinism on an injected clock |

Run: `pnpm --filter @track-site/web test -- living-ai-core assistant-panel`.
