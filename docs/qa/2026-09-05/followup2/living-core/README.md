# Task MS — Living AI Core budget evidence on the rebuilt bundle (2026-09-05, follow-up 2)

Build `9ZJAGzR3pMhmuGkmeGTk8` (task FX fixes, not rebuilt here) served with `next start -p 3016` (`server-3016.log`), stored owner session, chromium headless shell with `--use-angle=d3d11`. `summary.md` is generated by `node apps/web/scripts/qa/living-core-report.mjs --dir docs/qa/2026-09-05/followup2/living-core` from the JSON artifacts below and ends with the hand-written `summary-notes.md` (analysis, status table, harness changes, limits). Every number in `summary.md` points to one of these files.

| Artifact | Produced by | Content / result |
| --- | --- | --- |
| `lighthouse/summary.md`, `summary.json` | `living-core-lighthouse.mjs --runs 3 --configs off,system,full --variants docked` | medians `off` 81 / `system` 80 / `full` 80 → delta **−1 / −1 points** (target ≤ 3: pass); TBT 107 / 99 / 95 ms; LCP 4.69 / 5.00 / 5.01 s |
| `lighthouse/app--{off,system,full}--docked--run{1,2,3}.report.{json,html}` | Lighthouse 13.4.1, mobile preset, screen 1280 × 800 @ DPR 2, cookie header redacted | 9 raw reports |
| `lighthouse/tier-checks.json`, `run.log` | Playwright under the same emulation, polled up to 15 s | `off` → `static`; `system` → `css` (coarse pointer = constrained); `full` → `webgl` 3105 ms after load; device hints 8 cores / 32 GiB |
| `lighthouse/lcp-elements.json` | scratch script over the reports (`lcp-breakdown-insight`) | same LCP text element in all 9 runs; observed TTFB + render delay per run |
| `lighthouse-stdout.log` | runner stdout | run timeline 15:55:39 → 15:58:18 UTC |
| `longtasks-d3d11.json`, `longtasks.log`, `longtasks-stdout.log` | `living-core-budget.mjs --mode longtasks --idle-s 120 --scroll-s 60 --type-s 60` | **0 long tasks** in idle 120 s, scroll 60 s (253 wheel events), typing 60 s (1280 chars); tier `webgl` throughout; rAF callback p95 0.3 ms |
| `longtasks-d3d11.trace-summary.json` | scratch script over the 341 MB Chrome trace (trace itself outside the pack, path in the JSON) | 158 971 main-thread tasks, none ≥ 32 ms, longest 18.5 ms (`focusin`), per-frame cost `FireAnimationFrame` mean 0.22 ms |
| `soak.json`, `soak-samples.csv`, `soak.log`, `soak-stdout.log` | `living-core-budget.mjs --mode soak --soak-min 30 --sample-s 60 --step-s 20` | 30.0 min, 89 steps / 44 stubbed turns, all states except `approval_required`; GC'd heap 7.58 MB (min 5) → 9.21 MB (end) for 37 → 99 messages; listeners 390 → 391, ledger 327 constant; 1 canvas / 1 WebGL2 context / 0 lost; 0 long tasks after load (one 83 ms task at 3.0 s = the WebGL upgrade) |
| `motion-off.json`, `motion.log` | `living-core-budget.mjs --mode motion` | reduced motion and `off` (toggle, reload): 0 rAF in 3 × 10 s windows, static tier, SSR static, context released; preference restored to `system` |
| `server-3016.log` | `pnpm --filter @track-site/web start -p 3016` | server output; stopped at the end, port free |

Not run (unchanged): hidden-tab frames in a browser, real devices, Firefox / Edge, 200 % zoom, visual comparison, INP. Harness fixes in `apps/web/scripts/qa/living-core-{budget,lighthouse,report}.mjs` are listed in `summary-notes.md`; no commit.
