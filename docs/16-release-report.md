# Track redesign — release report and evidence pack (2026-09-05)

Closing report of the Track redesign programme (`docs/11-track-redesign-program.md`, phases 1–7) against the owner supplement §10 (responsive QA, accessibility, performance) and §11 (binding tests, definition of done, "Abschlussbelege" 1–8). Every number in this report is copied from a file in `docs/qa/2026-09-05/` or from a command output stored there; nothing is estimated. A check that could not run is listed as "not run" with the reason, never as passed.

| | |
| --- | --- |
| Before (pre-redesign) | commit `0f0f5b5` ("docs: design system reference for the Track redesign"), worktree `C:/Users/Soheil/Downloads/track-site-before`, `BUILD_ID` `sb9bxPyxbPMYA9tK0DYEV` |
| After (redesign) | commit `85fe3b7` on `feat/ai-tag-manager-platform` **plus the uncommitted working tree** of task F1 (fixes for the QA findings) and the QA tooling — see `docs/qa/2026-09-05/changed-files.md` §3; builds `rCAJOqYs841hSlnLSc99W` (85fe3b7 clean, S1–S4 evidence), `UANQbZ2DkEqCtTt7EriZY` (F1 re-check), `8k-9hBUufCX88fLKrM1Yp` (gate build of this report, `docs/qa/2026-09-05/reports/build.txt`) |
| Follow-up run (2026-09-05, afternoon) | HEAD `3da47f5` **plus the uncommitted working tree** of the follow-up tasks PERF (D9, changes C1–C11), E1 (Living AI Core budget harness), E2 (cross-browser matrix, optional `firefox` / `webkit` Playwright projects) and E3 (D15 contrast fixes, D16 keyboard fix + state specs): 39 modified tracked files (+545 / −459) and 18 untracked paths (`git status --short`); measurement build `JkuZkqiqEn0HgN4FIDCyT` (PERF "after", served for E1 and E2 and for the E3 "before" runs), throw-away build `2p2BhUEe9szAqx7yJQBwy` (E3 verification, `NEXT_DIST_DIR=.next-e3`, deleted), gate build of §11.1 (`docs/qa/2026-09-05/followup/reports/build.txt`). Results folded into §6.3–§6.6, §9 and §11.1; evidence under `docs/qa/2026-09-05/followup/` (305 files, 32 WebP, none above 150 KB) |
| Follow-up run 2 (2026-09-05, evening) | HEAD `caab018` (the first follow-up committed — D22 closed) **plus the uncommitted working tree** of the follow-up-2 tasks FX (D17 upgrade gate `living-ai-core/upgrade-gate.ts` + device rule, D18 CSP gating on the origin scheme in `apps/web/next.config.ts`, D20 hydration wait in `e2e/auth.setup.ts` / `e2e/app.spec.ts`, D21 runtime viewport key in `apps/web/src/app/app/layout.tsx`) and MS (Living AI Core budget measurements, harness fixes in `apps/web/scripts/qa/living-core-*.mjs`): 14 modified tracked files (+601 / −144) and 4 untracked paths (`git status --short`); measurement build `9ZJAGzR3pMhmuGkmeGTk8` (FX gates `docs/qa/2026-09-05/followup2/fx/build.log`, served on port 3016 for every MS measurement), gate build of §11.2 (`docs/qa/2026-09-05/followup2/reports/build.txt`). Results folded into §0, §6.6, §9 and §11.2; evidence under `docs/qa/2026-09-05/followup2/` (`fx/`, `living-core/`, `reports/`) |
| Evidence pack | `docs/qa/2026-09-05/` (`README.md` = baselines, rules, reproduction; the actual layout is listed there under "Actual layout") |
| Gates | `docs/12-design-system.md` §6: WCAG 2.2 AA, Lighthouse ≥ 95 accessibility / best practices / SEO, performance as close to 95 as realistic, LCP ≤ 2.5 s, INP ≤ 200 ms, CLS ≤ 0.1, 320–1920 px without horizontal scroll |

## 0. Result at a glance

| Gate / deliverable | Status | Evidence |
| --- | --- | --- |
| 1 Changed files and routes | done | §1, `docs/qa/2026-09-05/changed-files.md` |
| 2 Before/after screenshots 375 / 768 / 1440 / 1920 | done (25 pages, 511 WebP) | §2, `docs/qa/2026-09-05/before-after/index.md` |
| 3 Language and article parity | pass — 0 gaps in 5 locales vs English, 30 × 6 articles published | §3, `docs/i18n-parity-report.md` |
| 4 Redirect matrix Blog → Tracking Knowledge | pass — 224 rows, 58 fetched (0 failures), 166 by verified pattern rule | §4, `docs/qa/2026-09-05/seo/redirect-matrix-six-locales.md` |
| 5 Pricing / entitlement matrix | pass — catalogue-generated, 61 unit tests + 3 integration tests, live Stripe verification `billing: ok` | §5, `docs/qa/2026-09-05/pricing-matrix.md` |
| 6a Responsive 320–1920 | pass after F1 — 144 re-check runs: 0 horizontal scroll, 0 clipped, 0 hidden primary actions (was 5 / 63 / 57 of 336) | §6.1 |
| 6b axe WCAG 2.2 AA | pass — 0 violations in 24 re-check runs (was 2 serious nodes in 96 runs) | §6.2 |
| 6c Keyboard focus | pass — 120 / 120 tab stops with a visible indicator (was 116 / 120) | §6.2 |
| 6d Lighthouse a11y / BP / SEO ≥ 95 | pass on every public indexable page (100 / 96–100 / 100); `/en/login` and `/app` SEO 63 / 54 are `noindex` by design | §6.3 |
| 6e Lighthouse performance ≈ 95 (mobile) | **below target**: 87–94 after F1 (was 72–83); follow-up PERF (medians of 3): 91 / 91 / 95 / 92 on `/en` / pricing / hub / article (was 89 / 89 / 92 / 94 on the same method) — the hub reaches 95, the rest stays 91–92 | §6.3, defect D9 |
| 6f Core Web Vitals (lab, mobile) | LCP **fail** 2.92–3.99 s (target ≤ 2.5 s; was 4.45–4.93 s); follow-up medians 2.86–3.50 s (FCP −0.30 s on every page, HTML −52 to −55 KB); CLS pass 0–0.029; INP not measurable in a lab run (TBT proxy 33–79 ms, follow-up 42–57 ms) | §6.4, defect D9 |
| 6h Living AI Core budget (docs/15 §4) | **pass on the measurable criteria after follow-up 2** (build `9ZJAGzR3pMhmuGkmeGTk8`): static-vs-animated mobile Lighthouse **−1 point** for `system` and for `full` (medians 81 / 80 / 80, target ≤ 3 — was −6, D17 fixed by the upgrade gate); idle long tasks **0** in 120 s idle + 60 s scroll + 60 s typing on the `webgl` tier; 30-minute soak: GC'd heap +1.63 MB for +62 transcript messages, listeners flat (390 → 391), 1 canvas / 1 WebGL2 context / 0 lost; reduced-motion / `off` → 0 animation frames (re-confirmed); tier fallbacks and ≤ 30 fps in Chromium and WebKit (first follow-up). Open observations: simulated LCP +315 / +320 ms (observed FCP / render delay unchanged), one 83 ms task at the WebGL upgrade (3.0 s after navigation, outside load and idle windows). Still not run: hidden-tab frames in a browser, real devices, Firefox / Edge, 200 % zoom, visual comparison | §6.6, `docs/qa/2026-09-05/followup2/living-core/summary.md`, defects D10 (reduced), D17 (fixed) |
| 6i Cross-browser matrix | Chromium 40 / 40 + 12 / 12 visual + 5 / 5 core + 3 / 3 mobile; WebKit 38 / 39 functional through a header-strip hook (B1), 9 / 12 visual, 5 / 5 core with one console message (B3); Firefox **n/a** (cannot start here, B2); real devices / Safari macOS / Edge not run. Follow-up 2: the three tooling findings behind the WebKit column are fixed in code (D18 CSP directive gated on the origin scheme — `routes-manifest-csp.txt` shows no `upgrade-insecure-requests` for an http build; D20 sign-in specs wait for hydration; D21 viewport key added at runtime on Chromium only) and verified by unit tests / build greps / jsdom probes — **a WebKit re-run without the hook is still owed** | §6.6, defects D18–D21, `docs/qa/2026-09-05/followup2/fx/README.md` |
| 6j Contrast review of the axe "incomplete" nodes (D15) | **pass after E3**: 1 935 evaluations — 48 failing (38 nodes, 5 root causes) fixed in tokens / diagram / pricing diagram / inbox → 1 935 pass; dark-theme dashboard 204 / 204 | §6.2, defect D15 (fixed) |
| 6k Mobile keyboard vs composer, chat state across routes (D16) | keyboard: **defect found and fixed** (last message hidden below the shrunken viewport) with a permanent e2e; route change and site switch: pass with permanent e2e; job state and a real device keyboard still not exercised | §6.6, defect D16 (partly fixed) |
| 6g Visual regression | pass — 12 baselines, 4 verify runs × 12 passed | §6.5 |
| 7 typecheck / unit / contract / integration / production build | pass (typecheck 17 tasks; 823 unit tests; 63 contract tests; 22 integration tests; 715 / 715 pages) | §7 |
| 7 lint | pass after the final verification (§11): the 14 errors in `apps/web/scripts/qa/*.mjs` were fixed in place, `pnpm lint` exit 0, 17 / 17 tasks (`reports/final/lint.txt`; the failing run is kept as `reports/final/lint-before-fix.txt`) | §7, §11, defect D6 (fixed) |
| 7 SEO gate (`pnpm seo:check`) | **exit 0** after the fixes of 2026-09-05 (336 pages in six locales, redirects, robots, sitemap index and feeds OK) — the checker now measures decoded text lengths, counts only the `<head>` title and accepts TechArticle; evidence `docs/qa/2026-09-05/reports/final/seo-check-after-d1-d5.txt` | fixed (D5) |
| 7 e2e (Playwright chromium) | pass — 28 / 28 twice (one flaky reduced-motion test in a third run) + 12 new responsive/focus specs; not re-run in this task (no server) | §7 |
| Social cards of Tracking Knowledge pages | **pass** after the fix of 2026-09-05 — the cards are served by stable route handlers `…/tracking-knowledge/card.png` and `…/tracking-knowledge/<slug>/card.png` (200, image/png, localized `og:image:alt`, `twitter:image`); verified locally for hub and article, unknown slugs answer 404 | fixed (D1) |
| 8 Open points (external) | Resend key + vault on the Fly worker, Stripe test purchase, vendor OAuth apps, legal review of the imprint texts, first real vendor/shop test events | §8 |

The programme is complete as far as the repository can make it. Defects D1, D2c, D5, D7, D8 and D13 were fixed on 2026-09-05 (see §9); the release ships with the documented mobile performance gap (D9) and the owed evidence items D10/D15/D16, which the follow-up run addresses.

Follow-up run of the same day (§11.1): D9 reduced but not closed (medians 91 / 91 / 95 / 92, LCP 2.86–3.50 s — the remaining critical bytes are the Next.js/React runtime, the two web fonts and the site-wide stylesheet, §6.3); D15 fixed and verified (§6.2); D16 fixed for the keyboard case and covered by permanent specs for route changes and site switches (§6.6); D10 partly produced — the static-vs-animated comparison **misses** the ≤ 3-point criterion by 6 points (new defect D17), the idle long-task check and the 30-minute soak are still not run, the cross-browser matrix exists for Chromium and WebKit only (Firefox cannot start on this machine, D19). New tooling defects from the matrix: the build-time `upgrade-insecure-requests` directive blocks every WebKit check of a local build (D18), the sign-in spec races hydration (D20), the dashboard viewport meta logs a console error in WebKit (D21). The follow-up changes are uncommitted (D13 reopened as D22).

Follow-up run 2 of the same day, evening (§11.2; evidence `docs/qa/2026-09-05/followup2/`): D17 **fixed and re-measured** — the WebGL tier is now an upgrade requested only after the load event, ≥ 3 s since navigation start and an idle callback, with constrained / coarse-pointer devices staying on the CSS tier for `system` (docs/15 §2); on the rebuilt bundle the animated panel is within **1 point** of the static one (`off` 81, `system` 80, `full` 80; TBT 107 / 99 / 95 ms). The owed D10 measurements were produced: **0 long tasks** in 120 s idle, 60 s scrolling and 60 s typing on the `webgl` tier; the **30-minute soak** shows bounded growth that tracks the transcript (+1.63 MB GC'd heap for +62 messages, listeners 390 → 391, one canvas and one context throughout); reduced motion / `off` re-confirmed at 0 frames. D18, D20 and D21 are fixed in code (CSP directive gated on the `HOST_MARKETING` scheme, hydration wait in the sign-in specs, viewport key appended at runtime on Chromium only) but not yet re-verified in WebKit. D22 is closed (commit `caab018`); the follow-up-2 changes are in turn uncommitted (14 modified files, 4 untracked paths).

## 1. Changed files and routes

Generated by `docs/qa/2026-09-05/changed-files.mjs` → `docs/qa/2026-09-05/changed-files.md` (summary by area, the 23 commits, the uncommitted working tree, route tables from the two build manifests, and every file). `git diff --stat 0f0f5b5..HEAD`: **1 008 files changed, 117 827 insertions, 6 616 deletions** (772 added, 139 modified, 31 deleted, 66 renamed). On top of HEAD the working tree carries the F1 fixes and the QA tooling: 40 paths, 35 modified tracked files with 344 insertions / 103 deletions (`git diff --stat`), plus the untracked `apps/web/e2e/visual.spec.ts`, `apps/web/e2e/README.md`, `apps/web/e2e/__screenshots__/` (12), `apps/web/scripts/qa/` (3) and `docs/qa/`.

Largest areas (files, + / − lines; full table in `changed-files.md` §1):

| Area | Files | + | − |
| --- | ---: | ---: | ---: |
| Tracking Knowledge articles + learning paths ×6 (`apps/web/content/knowledge`) | 186 | 10 688 | 132 |
| UI message catalogs ×6 (`apps/web/messages`) | 100 | 27 644 | 78 |
| Marketing copy modules ×6 (`apps/web/src/lib/marketing-copy`) | 93 | 13 084 | 0 |
| Dashboard module components (`apps/web/src/components/app/**`, without the shell) | 90 | 10 830 | 301 |
| Public routes under `/[locale]` (marketing, knowledge, auth, metadata) | 57 | 2 183 | 1 445 |
| Web server (data access, actions, billing, entitlements) | 57 | 13 022 | 159 |
| Marketing components (header, footer, home, features, integrations, auth shell) | 47 | 4 426 | 282 |
| Dashboard routes `/app/**` | 44 | 2 997 | 852 |
| Dashboard shell (viewport-fixed layout, Track AI panel, Living AI Core, palette) | 28 | 3 885 | 0 |
| `packages/db` (migrations 0004–0013, repositories, seed) | 28 | 2 026 | 93 |
| `packages/ui` (tokens, primitives, brand) | 24 | 2 261 | 184 |
| Track AI chat (store, reducer, virtual list, workspace moves) | 23 | 2 663 | 314 |
| Tracking Knowledge components | 21 | 2 236 | 0 |
| `packages/ai` (UI event contract, scope gate, evals) | 20 | 2 028 | 37 |
| Interactive hero demo | 19 | 1 667 | 0 |
| Pricing components | 17 | 1 567 | 0 |
| `packages/catalog` (new tariff catalogue) | 13 | 1 120 | 0 |

Routes (`apps/web/.next/app-path-routes-manifest.json`, before 64 → after 86 routes; `changed-files.md` §4):

| Group | Before (`0f0f5b5`) | After |
| --- | --- | --- |
| Public pages ×6 locales | `/`, `/de`, `/[locale]/{features, features/[slug], how-it-works, integrations, integrations/[slug], pricing, docs, support, contact, demo, status, security, privacy, terms, data-processing, subprocessors, imprint}`, auth pages; `en` unprefixed, `de` prefixed | the same pages under `/[locale]/(marketing)` and `/[locale]/(auth)` for `en, de, fr, es, it, nl` (`localePrefix: "always"`; `/` and every unprefixed marketing URL 308 → `/en/...`, query preserved) |
| Blog → Tracking Knowledge | `/[locale]/blog`, `/[locale]/blog/[slug]`, `/[locale]/blog/feed.xml` | `/[locale]/tracking-knowledge`, `/[locale]/tracking-knowledge/[slug]`, `/[locale]/tracking-knowledge/feed.xml`, generated social cards `…/opengraph-image-1vcy9h` (hub) and `…/[slug]/opengraph-image-qnehfz` (article), `/[locale]/opengraph-image` (pages); old URLs answered by permanent redirects only (§4) |
| Sitemaps / metadata | `/sitemap.xml`, `/robots.txt`, `/icon.svg` | `/sitemap.xml` (index) + `/sitemaps/[name]` (`pages-<locale>.xml`, `knowledge-<locale>.xml` ×6), `/robots.txt`, `/icon.svg`, `/apple-icon.png`, `/manifest.webmanifest` |
| Dashboard | `/app`, `/app/debugger`, `/app/audiences`, `/app/events`, `/app/destinations`, `/app/data-quality`, `/app/consent`, `/app/team`, `/app/billing`, `/app/settings`, `/app/sites/**`, `/app/onboarding/**` | `/app` (Command Center), `/app/ai-setup`, `/app/events` + `/explorer` + `/matrix` + `/test-lab`, `/app/destinations`, `/app/data-quality` + `/revenue-leaks`, `/app/consent` + `/simulator`, `/app/insights` + `/attribution` + `/audiences`, `/app/releases` + `/[versionId]`, `/app/team` + `/audit`, `/app/billing` + `/usage`, `/app/settings` + `/alerts`, `/app/sites/**`, `/app/onboarding/**`; `/app/setup` → 308 `/app/ai-setup`, `/app/debugger` → 308 `/app/events/explorer`, `/app/audiences` → 308 `/app/insights/audiences` (crawl-verified, `seo/summary.md`) |
| API | `/api/ai/{chat,confirm,credential,health,wizard}`, `/api/auth/[...all]`, `/api/health`, `/api/oauth/[provider]/callback`, `/api/privacy/dsar/[id]`, `/api/stripe/webhook`, `/cdn/v1/c/[...path]` | + `/api/ai/dev-fixture` (dev only, 404 in production), `/api/app/events/explorer`, `/api/app/events/test-lab/[runId]`, `/api/knowledge/feedback` |

Prerendered output: 661 static routes (107 per locale + 19 shared) and 22 dynamic patterns (`prerender-manifest.json`); the build log reports 715 / 715 static pages (`reports/build.txt`).

## 2. Before / after screenshots (375, 768, 1440, 1920 px)

`docs/qa/2026-09-05/before-after/index.md` — one table per page with the four widths side by side, page heights, HTTP status and a factual change description; `capture.json` (URL, status, final URL, page height, captured width, file sizes, WebP quality, document-overflow flag per capture) and `mapping.json` (route pairing). 511 WebP files, every file ≤ 150 KB (largest 150 KB, verified by decoding), long pages split into `-partN` segments in reading order. Both sides are running production builds (`next start`): before `0f0f5b5` on :3004, after `85fe3b7` on :3005 (`AI_DEV_FIXTURES=1`), Playwright Chromium, `deviceScaleFactor` 1, full page, default motion preference.

| Page | Directory | Notes |
| --- | --- | --- |
| Home en / de | `before-after/home-en/`, `home-de/` | brand `track.site` → `Track`, dropdown navigation + six-language switcher, static preview card → interactive demo (Overview / Live Events / Destinations / AI Setup / Attribution), new section order, page height at 1440 px 4 032 → 8 028 px |
| Home fr / es / it / nl | `home-fr/`, `home-es/`, `home-it/`, `home-nl/` | after only (locales did not exist before) |
| Pricing en / de / fr / es / it / nl | `pricing-en/` … `pricing-nl/` | after: 3 plan cards + Enterprise panel, toggle, plan finder, calculator, comparison matrix, FAQ, tax note; **before renders HTTP 500** because the before tree reads the shared database migrated to the after schema (`index.md` "Limitations") |
| Knowledge hub en / de | `knowledge-en/`, `knowledge-de/` | blog card grid → hub with search, learning paths, cover family |
| Article `consent-mode-v2-guide` en / de | `article-consent-mode-v2-guide-en/`, `-de/` | new article template (TOC, reading progress, takeaways, callouts, feedback, print) |
| Features, integrations (en) | `features-en/`, `integrations-en/` | product views / searchable catalogue |
| Login, signup en / de | `login-en/`, `login-de/`, `signup-en/`, `signup-de/` | focused auth shell |
| Dashboard `/app`, `/app/events/explorer`, `/app/releases` | `app-overview/`, `app-events-explorer/`, `app-releases/` | viewport-fixed shell with Track AI panel; `/app/releases` after only (404 before) |

Horizontal overflow recorded during the captures (`index.md` "Horizontal overflow observed"): 18 before captures (the old site at 768 px and the old article at 375 px) and 6 after captures (dashboard shell at 375 / 768 px, `/de` and `/nl` pricing at 768 px). The after cases are findings F1 and F3 of the responsive sweep and are fixed by F1 (§6.1); the before/after WebPs deliberately show the state of `85fe3b7` before the fixes, the re-check screenshots of the fixed build are under `docs/qa/2026-09-05/recheck/screenshots/`.

## 3. Language and article parity

`docs/i18n-parity-report.md` / `.json` (copies in `docs/qa/2026-09-05/seo/`), generated 2026-09-05T09:13:52Z by `node apps/web/scripts/i18n-parity.mjs --strict` (exit 0). Reference English: 3 808 message keys in 17 namespaces, 21 typed copy modules, 30 knowledge topics (30 published), 4 learning paths, 95 catalogue labels.

| Locale | Active | Message keys (gaps) | Copy modules (gaps) | Knowledge labels | Legal | Mail | Knowledge published / topics | Learning paths (gaps) | Catalogue labels (gaps / 95) | Status |
| --- | --- | ---: | ---: | ---: | ---: | ---: | --- | ---: | --- | --- |
| en | yes | reference | reference | reference | reference | reference | 30 / 30 | reference | reference | reference |
| de | yes | 0 | 0 | 0 | 0 | 0 | 30 / 30 | 0 | 0 / 95 | complete |
| fr | yes | 0 | 0 | 0 | 0 | 0 | 30 / 30 | 0 | 0 / 95 | complete |
| es | yes | 0 | 0 | 0 | 0 | 0 | 30 / 30 | 0 | 0 / 95 | complete |
| it | yes | 0 | 0 | 0 | 0 | 0 | 30 / 30 | 0 | 0 / 95 | complete |
| nl | yes | 0 | 0 | 0 | 0 | 0 | 30 / 30 | 0 | 0 / 95 | complete |

Corroborating evidence:

- `docs/qa/2026-09-05/seo/knowledge-validate.txt` (`pnpm --filter @track-site/web knowledge:validate`, exit 0): 180 files, 30 translation groups, locales en/de/fr/es/it/nl, learning paths and featured story valid; front matter check: 0 fields to change, 0 internal `/blog` links left.
- Crawl of the production build (`docs/qa/2026-09-05/seo/summary.md`, `crawl.json`): 12 sitemaps (`pages-<locale>.xml` 48 URLs, `knowledge-<locale>.xml` 30 URLs each), 468 public URLs crawled, 468 / 468 with 7 hreflang links (six locales + `x-default`), 468 / 468 with a self-canonical and a meta description, 6 RSS feeds with 30 items each, no `/blog/` URL in any sitemap or feed; `<html lang>` correct on the 336 pages of the SEO gate (`seo/seo-check.txt` reports no `lang` finding).
- Catalogue labels: `docs/qa/2026-09-05/pricing-matrix.md` §11 — 76 catalogue labels × 6 required locales, 0 missing.
- Playwright `marketing.spec.ts`: "serves all six programme locales" and home ×6 with the right `lang`, one `h1`, hreflang ×7 and no serious axe violation; language switcher stays on `/pricing` en → de → fr (`reports/e2e/e2e-run4.log`, 28 passed).
- Machine-readable parity per `translationGroupId`: `docs/i18n-parity-report.json` (knowledge section per locale) and the validator's 30 groups × 6 locales.

Not measured: automatic language detection of rendered text (mixed-language pages are prevented structurally — `pick()` throws on a missing copy entry and the parity script is strict — but no language classifier ran over the HTML).

## 4. Redirect matrix Blog → Tracking Knowledge

- Rules: `apps/web/src/lib/routes.ts` `KNOWLEDGE_LEGACY_REDIRECTS`, applied as permanent redirects by `apps/web/next.config.ts` before the locale proxy: `/blog` → `/en/tracking-knowledge`, `/blog/feed.xml` → `/en/tracking-knowledge/feed.xml`, `/blog/:slug` → `/en/tracking-knowledge/:slug`, and `/:locale(en|de|fr|es|it|nl)/blog[/feed.xml|/:slug]` → `/:locale/tracking-knowledge[...]`; explicit per-article rules would run first if a localized slug differed from the old shared slug (none needed: `KNOWLEDGE_SLUG_REDIRECTS` is empty and every localized slug equals the English file name). Next.js preserves query strings.
- Matrix: `docs/qa/2026-09-05/seo/redirect-matrix-six-locales.md` (generated by `seo/redirect-matrix-six-locales.mjs` from the 180 article files and the active locales) — **224 rows**: 2 unprefixed index/feed, 12 locale index/feed, 30 unprefixed article rows, 180 locale article rows. `docs/redirects-blog-to-tracking-knowledge.md` (generated 2026-09-03 by `apps/web/scripts/redirect-matrix.mjs` when only en/de were active) holds the 96-row subset; regenerating it is defect D8.
- Verification (crawl of the production build, `seo/crawl.json` → `redirectMatrix`): 46 matrix rows fetched (all 6 index/feed rows + a deterministic sample of 40 article rows) and 19 additional checks (fr/es/it/nl index, feed and one article each; `?utm_source=qa&utm_medium=crawl` and `?category=guides` preserved; `/` → `/en`, `/pricing` → `/en/pricing`; the three dashboard legacy paths): **every row answers exactly one 308 whose Location equals the documented target and the target answers 200 — 0 failures, 0 redirect chains**. In the six-locale matrix that is 58 rows verified by fetch and 166 rows covered by a pattern rule that was verified for the same locale group and URL shape; 0 rows unchecked, 0 failed.
- Playwright `marketing.spec.ts` "old blog URLs redirect permanently and directly to Tracking Knowledge, query string included" and "unprefixed URLs redirect permanently to English and keep the query string" (28-test runs, `reports/e2e/e2e-run2.log`, `e2e-run4.log`).
- Link integrity of the new site (crawl): 960 unique internal link/image targets, 0 redirect chains, exactly one link answered via a single redirect (`/contact?topic=enterprise` linked from `/app/billing` → `/en/contact?topic=enterprise`, defect D2c); the 186 "broken image" targets are all the `og:image` URLs of defect D1.

## 5. Pricing and entitlement matrix

`docs/qa/2026-09-05/pricing-matrix.md`, generated by `docs/qa/2026-09-05/pricing-matrix.mjs`, which imports `@track-site/catalog` (`packages/catalog/src/*.ts`) and renders every table from the exported objects and functions: plans and list prices, hard limits, the 42 feature gates × 4 plans (cumulative), overage packs, policies, warning thresholds and grace window, trial, billable-event definition, Stripe price slots with `verifyStripeAmount` examples, deterministic plan-finder and cost-calculator cases, database plan records, label coverage and the test evidence.

| Plan | Monthly | Yearly (= 10 × monthly) | Sites | Events / month | Team | Retention | Overage pack | Recommended / sales |
| --- | ---: | ---: | ---: | ---: | ---: | --- | --- | --- |
| Starter | 19 € | 190 € | 1 | 500 000 | 2 | 90 days | 100 000 events for 6 € (opt-in) | — |
| Growth | 90 € | 900 € | 5 | 5 000 000 | 10 | 13 months (396 days) | 1 000 000 events for 18 € (opt-in) | recommended |
| Pro | 180 € | 1 800 € | 25 | 20 000 000 | unlimited (fair use) | 25 months (761 days) | 5 000 000 events for 30 € (opt-in) | — |
| Enterprise | custom | custom | contract | contract | contract | contract | contractual | contact sales |

Overage policies `allow` / `cost_limit` / `pause` (default `pause`, never activated without a choice), warnings at 70 / 90 / 100 %, `pause` grace 20 %; trial: Growth, 14 days, no card, 100 000 accepted events, no auto-conversion, read-only + export after expiry; billable = accepted by ingestion, counted once, fan-out never counts, six non-billable reasons.

Automated evidence (`pricing-matrix.md` §12; results in `docs/qa/2026-09-05/reports/test.txt` and `test-integration.txt`):

| Test | Count | Result |
| --- | ---: | --- |
| `packages/catalog/src/catalog.test.ts` (four plans, binding prices in cents / yearly = 10 × monthly, entitlements per supplement §5, cumulative features with labels in every required locale, strict labels, overage/thresholds/trial, DB records with PRO env names, billable rules, plan finder determinism, cost calculator incl. yearly and contractual overage, Stripe slots + deprecated SCALE fallback, amount verification, label locales) | 20 | passed |
| `apps/web/src/components/marketing/pricing/pricing-helpers.test.ts` (EUR formatting per locale without invented decimals, validated signup links, slider stops incl. every plan limit, deterministic finder mirroring `recommendPlan`, calculator with packs and honest upgrade hint, whole yearly instalments) | 13 | passed |
| `apps/web/src/components/marketing/pricing/plan-selection.test.ts` (pricing → signup hand-over round trip) | 10 | passed |
| `apps/web/src/server/usage.test.ts` (usage guard: thresholds 70/90/100, forecast, hard limit mirroring the worker — `allow` never pauses, `pause` at 120 %, `cost_limit` by pack cost — pack maths, cheaper-upgrade advice, contractual Enterprise) | 18 | passed |
| `apps/web/src/server/usage.integration.test.ts` (DB-backed ledger) | 3 | passed (serial run) |
| Health price verification: `/api/health` → `billingStatus()` resolves all six `STRIPE_PRICE_<PLAN>_<INTERVAL>` slots through `resolvePrice`, which rejects amounts/currencies that differ from the catalogue (`amount_mismatch` / `currency_mismatch`), and checks active / recurring / interval / tax behaviour | live | `reports/health-www-track-site.json` (fetched 2026-09-05T11:29:15Z): `billing: ok`, ok = all six slots, missing = [], failed = [], deprecated = [] → the live Stripe prices equal 19 / 190 / 90 / 900 / 180 / 1 800 € and the `PRO` env names are in use (docs/11 §6 owner actions 1–3 done) |
| Playwright: `/en/pricing` one `h1` and no invented amounts; responsive `/fr/pricing` @320, `/de/pricing` @768, `/nl/pricing` @1024; visual baselines `pricing-375` / `pricing-1440` | 3 + 3 + 2 | passed (`reports/e2e/e2e-run4.log`, `recheck/e2e-new-specs.log`, `reports/e2e/visual-verify-final-*.log`) |

Supplement §11 "Pricing" checklist: exact 19 / 90 / 180 € monthly and 190 / 900 / 1 800 € yearly — catalogue test + live health + `before-after/pricing-en/after-1440-part1.webp`; Starter / Growth / Pro / Enterprise consistent between UI, checkout and entitlements — one catalogue drives `pricing.ts`, `billing.ts` (checkout refuses a mismatching price), `entitlements.ts`, the seed and the worker usage job (`packages/catalog` README block in `docs/11` §3); no "price not published" state — the pricing page renders catalogue prices without a Stripe dependency; plan finder and calculator deterministic — `catalog.test.ts` + `pricing-helpers.test.ts`; tax, billing and cancellation notes localized — `apps/web/src/lib/marketing-copy/pricing/{en,de,fr,es,it,nl}.ts` (`hero.facts`, `tax`, `billedMonthly`), parity 0 gaps.

## 6. Accessibility, Lighthouse and Core Web Vitals

### 6.1 Responsive QA (supplement §10)

Sweep script `apps/web/scripts/qa/responsive-a11y-sweep.mjs` (Playwright Chromium, axe-core 4.13, sharp), production build, 320 / 375 (mobile emulation) / 768 / 1024 / 1440 / 1920 px. Per route × width: `document.scrollWidth` vs `clientWidth`, `visualViewport.scale`, dashboard `main` overflow, elements wider than the viewport outside scroll containers, clipped headings/buttons/links, reachability of the primary action, screenshots ≤ 150 KB.

| Run | Routes × widths | Horizontal page scroll | Dashboard main overflow | Runs with elements wider than the viewport | Clipped headings / buttons | Primary action not reachable | Report |
| --- | --- | ---: | ---: | ---: | ---: | ---: | --- |
| S1 on `rCAJOqYs841hSlnLSc99W` | 56 × 6 = 336 (15 public pages en + de, home / pricing in all six locales, 18 dashboard views) | 5 | 7 | 63 | 0 | 57 | `docs/qa/2026-09-05/screenshots/responsive-sweep.md`, `responsive-findings.md` (F1–F10), 633 route screenshots |
| F1 re-check on `UANQbZ2DkEqCtTt7EriZY` | 144 (18 `/app` routes at 320 / 375 / 768, six of them at all widths, home ×6 locales, pricing ×4, article en/de) | **0** | **0** | **0** | **0** | **0** | `docs/qa/2026-09-05/recheck/screenshots/responsive-sweep.md`, `recheck/README.md` §1 |

Root causes fixed by F1 (`recheck/README.md` §1, uncommitted working tree): dashboard shell grid without an explicit column (implicit `auto` track sized to the header's min-content → `grid-cols-[minmax(0,1fr)]`, shrinkable header, drawer entry for the palette below `sm`), `grid-auto-columns: minmax(0,1fr)` for dashboard grids, hero demo switched to container queries and a full-width metric strip, `ScrollRegion` positioned relative with an edge fade (sr-only spans no longer escape the scroller), wrapping CTAs (`/fr` hero, `/fr/pricing` Enterprise panel, overage value), footer link wrapping (`/nl` at 1024), consent-simulator sticky form capped to the viewport. Regression specs: `apps/web/e2e/app.spec.ts` "responsive header" (320 / 375 / 768) and "keyboard focus in the header", `apps/web/e2e/marketing.spec.ts` "responsive layout" (7 locale × width cases) — 12 passed (`recheck/e2e-new-specs.log`).

Not covered: public routes other than home / pricing / article were not re-swept after F1 (their code paths changed only by the footer and font fixes); full-page screenshots are capped at 6 000 px and split, so very long pages (home at 375 px ≈ 15 000 px) are only partly covered visually while the DOM checks cover the whole page; 200 % zoom was not tested as a separate state.

### 6.2 Accessibility (WCAG 2.2 AA)

| Check | S1 (`rCAJOqYs841hSlnLSc99W`) | F1 re-check (`UANQbZ2DkEqCtTt7EriZY`) | Files |
| --- | --- | --- | --- |
| axe-core, tags `wcag2a` + `wcag2aa` + `wcag22aa`, all impacts, widths 375 (touch) and 1440 | 96 runs (public en + de and dashboard): **2 serious nodes** (`scrollable-region-focusable` on the article table wrapper at 375 en/de), 0 critical / moderate / minor; 94 / 96 pages clean | 24 runs: **0 violations of any impact**, 24 / 24 clean | `axe/summary.md`, `axe/<slug>--<width>.json` (96 raw), `recheck/axe/summary.md` + raw JSON |
| Keyboard: 40 × Tab from load on `/en`, `/en/pricing`, `/app` at 1440; focused vs blurred computed styles; crop per stop | `/en` 40 / 40 visible, `/en/pricing` 40 / 40, `/app` 36 / 40 (workspace switcher triggers and account menu: Tailwind v4 `outline-none` cancelled `focus-visible:outline-2`) | **120 / 120 visible**, 0 not matching `:focus-visible` | `axe/keyboard-summary.md`, `axe/keyboard/*.json`, `screenshots/keyboard/<slug>/tab-NN.webp`; `recheck/axe/keyboard-summary.md` |
| Lighthouse accessibility | 100 on all 24 runs | 100 on all 8 runs | §6.3 |
| Playwright axe (`marketing.spec.ts`, home ×6 with `wcag2a`/`wcag2aa`) | 6 / 6 passed | — | `reports/e2e/e2e-run4.log` |
| Skip link, landmarks, one `h1` | skip link is tab stop 1 on every checked page; 496 / 498 crawled pages have exactly one `h1` (`/app/destinations` has two → defect D2a; `/app/onboarding/organization` is a 307) | — | `axe/keyboard-summary.md`, `seo/summary.md` |
| Reduced motion | e2e "prefers-reduced-motion renders the static tier from the server on" and "motion preference off produces no animated frame" passed (run 2 and 4; flaky once, see §7) | — | `reports/e2e/` |

Follow-up E3 (D15, `docs/qa/2026-09-05/followup/contrast/README.md`, `summary.md`, `contrast-review.mjs`): the 1 935 "incomplete" `color-contrast` evaluations (822 unique nodes, 73 runs of `axe/*--{375,1440}.json`) were re-evaluated on the same routes with a computed-style background walk (ancestor backgrounds, gradients and pattern stops, overlays under the sample point, SVG shapes painted before a text). Before the fixes (build `JkuZkqiqEn0HgN4FIDCyT`, `results.json`): 1 879 pass, **48 FAIL** (38 unique nodes), 8 not found (selectors changed by F1, resolved by tag + class + text). Five root causes, all fixed in the working tree: (1) `.surface-stage` did not re-scope `--color-cyan-strong` (nor violet-strong, violet-soft-2, cyan-soft-2), so diagram labels rendered the light cyan on the dark stage at 2.61:1 / 3.31:1 → `packages/ui/src/styles/tokens.css` (26 nodes; after 8.52:1 / 10.82:1); (2) dark / stage `--color-primary-soft` `#16264d` → `#101c40` for `text-primary` 4.20:1 and `text-ink-3` 4.39:1 (6 nodes; after 4.72:1 / 4.93:1); (3) `FlowNode` sublabels `fill-on-primary/80` 4.48:1 → `/90` in `packages/ui/src/diagram.tsx` (6 nodes; after 5.14:1); (4) the German pricing sublabel "angenommen · einmal gezählt" was wider than the 124 px Track node of the event-definition diagram (white on the ground, 1.06:1; es / it / nl overflow too) → locale-aware node width in `apps/web/src/components/marketing/pricing/event-definition.tsx` (after 5.56:1); (5) `text-primary/80` on the active `bg-primary-soft` chip 3.92:1 → `text-primary` in `apps/web/src/components/app/data-quality/inbox.tsx` and `packages/ui/src/primitives/search.tsx` (after 5.66:1). After (build `2p2BhUEe9szAqx7yJQBwy`, `results-after.json`): **1 935 pass, 0 FAIL, 0 not found**; dark theme on the 204 dashboard nodes: 204 pass (`results-dark.json`); every gradient and dot-pattern background keeps ≥ 4.5:1 against its darkest stop. Limits: one sample point per node, `url()` backgrounds not composited (none occurred), marketing pages not swept in dark mode.

Not verified: screen-reader announcements for chat stream, search, filters, tables and status changes were not tested with a screen reader (the chat feed uses a polite live region — e2e "exposes the activity live region"); menu items inside opened popovers were not tabbed through; browsers other than Chromium were not run.

### 6.3 Lighthouse (13.4.1)

Method: `apps/web/scripts/qa/lighthouse.mjs`; mobile = Lighthouse default mobile emulation with simulated throttling (Moto G Power class, 4 × CPU, 150 ms RTT / 1.6 Mbps), desktop = `--preset=desktop`; scores are `categories.*.score × 100`, metrics are `audits.*.numericValue`. S2 ran 3 mobile runs per page (medians reported) and 1 desktop run for `/en` and `/en/pricing`; the F1 re-check ran 1 mobile run per page (± 5 points of noise). Browser: Playwright's `chrome-headless-shell` 151 via `CHROME_PATH` because the full Playwright Chromium cannot start on this machine ("Side-by-Side-Konfiguration ungültig"); a cross-check with the installed Google Chrome 152 gave the same picture (`lighthouse/diagnostics/home-mobile-chrome152-crosscheck.report.json`: performance 59, LCP 5.33 s before the fixes).

| Page (mobile) | Perf before → after | A11y | BP before → after | SEO | LCP before → after | TBT before → after | CLS after | Target check (after) |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `/en` | 77 → **87** | 100 | 96 → 100 | 100 | 4.73 s → **3.99 s** | 162 → 58 ms | 0.000 | a11y / BP / SEO pass; perf and LCP below target |
| `/de` | 73 → **87** | 100 | 96 → 100 | 100 | 4.93 s → **3.96 s** | 317 → 67 ms | 0.000 | same |
| `/en/pricing` | 75 → **93** | 100 | 96 → 100 | 100 | 4.58 s → **3.09 s** | 310 → 38 ms | 0.029 | same |
| `/en/tracking-knowledge` | 80 → **87** | 100 | 96 → 100 | 100 | 4.61 s → **3.82 s** | 135 → 48 ms | 0.000 | same |
| `/en/tracking-knowledge/consent-mode-v2-guide` | 83 → **94** | 100 | 100 → 100 | 100 | 4.45 s → **3.01 s** | 107 → 33 ms | 0.019 | same |
| `/en/integrations` | 76 → **94** | 100 | 96 → 100 | 100 | 4.53 s → **2.92 s** | 229 → 38 ms | 0.000 | same |
| `/en/login` | 79 → **89** | 100 | 92 → 96 | 63 | 4.74 s → **3.62 s** | 237 → 79 ms | 0.000 | SEO 63 = `is-crawlable` fails because the page is `noindex` by design |
| `/app` (authenticated, stored session) | 72 → **89** | 100 | 100 → 100 | 54 | 4.82 s → **3.77 s** | 397 → 57 ms | 0.000 | SEO 54 = `noindex` + no description by design (dashboard) |
| `/en` desktop (S2 only) | 99 | 100 | 96 | 100 | 836 ms | 35 ms | 0.001 | all pass |
| `/en/pricing` desktop (S2 only) | 99 | 100 | 96 | 100 | 919 ms | 7 ms | 0.000 | all pass |

Sources: `docs/qa/2026-09-05/lighthouse/summary.md` + `summary.json` (24 raw `*.report.json/.html`, `run.log`), `docs/qa/2026-09-05/recheck/lighthouse/summary.md` + raw reports. What F1 changed for performance: fonts (`latin` subset only, Bricolage no longer preloaded — font transfer on `/en` 197 929 → 91 792 B, 4 → 2 requests) and the invalid SVG `height="auto"` that logged a console error per page with a diagram or cover (best practices 96 → 100). What remains and why (`recheck/README.md` §3): 16 hydration script files (217–231 KB gzip) of which 13.6 KB are Next.js's unconditional `@next/polyfill-module` and 22–29 KB React/Next runtime, 94–107 KB HTML documents, one site-wide render-blocking stylesheet (Tailwind v4 emits no per-route CSS), and the comparison matrix rendered twice (desktop table + mobile accordion, 2 251 DOM nodes on pricing) — defect D9.

#### 6.3.1 Follow-up PERF (D9, 2026-09-05 afternoon) — medians of 3 mobile runs

`docs/qa/2026-09-05/followup/perf/summary.md` (§2 tables generated by `compare.mjs` from `lighthouse-before/summary.json` and `lighthouse-after/summary.json`; 12 + 12 raw reports). Same method as above (Lighthouse 13.4.1, mobile preset, headless shell 151, port 3011) but **3 runs per page and medians**; before = build `VaXHIAlSHJGtV0jRKwjXB` of the tree at `3da47f5`, after = build `JkuZkqiqEn0HgN4FIDCyT` of the tree with the changes C1–C11 (message catalog out of the marketing RSC payload, error boundary and header copy without the ICU runtime, hero demo placeholder as a server component with an interactive island loaded on visibility, tariff catalogue out of the pricing hydration bundle, finder / calculator as a lazily hydrated island with a server-rendered initial state, one `<symbol>` pair instead of 336 inline matrix icons, integrations catalogue out of the hub client bundle, `marketing.css` without dashboard rules, cache headers for icons / manifest / brand / cards, analysis tooling — `summary.md` §3).

| Page (mobile) | Perf before → after (runs) | LCP | TBT | CLS | FCP | HTML transfer | Route chunks (gzip) | Target check |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `/en` | 89 (94/89/89) → **91** (91/91/91) | 3.63 → **3.45 s** | 38 → 57 ms | 0.000 | 1.66 → **1.37 s** | 92.7 → **40.5 KB** | 67.5 → **55.0 KB** | perf and LCP below target |
| `/en/pricing` | 89 (89/85/89) → **91** (91/90/95) | 3.64 → **3.50 s** | 41 → 48 ms | 0.029 | 1.82 → **1.51 s** | 102.5 → **49.7 KB** | 71.9 → **57.3 KB** | same |
| `/en/tracking-knowledge` | 92 (87/92/92) → **95** (95/91/95) | 3.17 → **2.86 s** | 39 → 44 ms | 0.000 | 1.81 → **1.51 s** | 104.5 → **49.5 KB** | 73.3 → **62.4 KB** | perf reaches 95; LCP below target |
| `/en/tracking-knowledge/consent-mode-v2-guide` | 94 (94/96/89) → **92** (92/92/91) | 3.02 → **3.35 s** | 36 → 42 ms | 0.019 | 1.66 → **1.37 s** | 80.3 → **25.6 KB** | 60.2 → **55.4 KB** | perf and LCP below target; the LCP median lies inside the before spread 2.71–3.73 s |

Accessibility / best practices / SEO stay 100 / 100 / 100 on all 12 after-runs (`lighthouse-after/summary.md`). The `/en` TBT and Speed Index medians (57 ms, 2.35 s) come from two runs in which the headless shell painted ~1–2 s after `load` (`observed-paint-per-run.txt`; the third run has 38 ms / 1.36 s). Why the LCP target is still missed (`summary.md` §5, from Lighthouse's Lantern source): on localhost every hydration script and both web fonts finish before the observed first paint, so the simulation places them ahead of the LCP — the floor is the 131.2 KB gzip Next.js/React runtime, 89.6 KB of fonts and the 21.6 KB site-wide stylesheet (≈ 330–365 KB on the 1.6 Mbps link), none of which the task could remove; the document and route chunks, which it could, are now 40–50 KB and 55–63 KB of that graph. Measured and not kept: `experimental.inlineCss` (document 40.5 → 80.0 KB, LCP not better), `experimental.optimizePackageImports`, Turbopack tree-shaking flags (byte-identical chunks). Remaining items identified but outside the task (`summary.md` §4, §8): the ICU message-format runtime (12.1 KB gzip, bundled with next-intl's client navigation), four unused `@track-site/ui` client primitives (≈ 2.5 KB) and barrel-retained server-safe modules (≈ 3–4 KB gzip; `packages/ui` declares no `sideEffects: false`), the comparison matrix still rendered twice, 30 inline hub covers. Behavioural note: a value typed into the server-rendered finder / calculator before the interactive chunk swaps in (load starts 200 px ahead and on the first focus / pointer contact) is not carried over (`pricing-tools-island.tsx`). Verification in that task: typecheck, lint, 500 unit tests, build 715 / 715, e2e 52 / 52 without baseline changes, `island-check.mjs` 12 / 12 (`summary.md` §6).

### 6.4 Core Web Vitals (lab values)

| Metric | Target (mobile) | After F1 (Lighthouse simulated, 1 run per page) | Before (S2 medians) | Status |
| --- | --- | --- | --- | --- |
| LCP | ≤ 2.5 s | 2.92 s (`/en/integrations`) … 3.99 s (`/en`) | 4.45 … 4.93 s | **fail** on every mobile page; desktop 0.84–0.92 s pass |
| INP | ≤ 200 ms | not measurable in a lab run without interactions; TBT proxy 33–79 ms (was 107–397 ms) | — | not run (INP) / proxy pass |
| CLS | ≤ 0.1 | 0.000–0.029 | 0.000–0.001 | pass |
| LCP, follow-up PERF (medians of 3, §6.3.1) | ≤ 2.5 s | **2.86 s** (hub) … **3.50 s** (pricing); FCP 1.37–1.51 s | 3.02 … 3.64 s (same method, build `VaXHIAlSHJGtV0jRKwjXB`) | **fail** on the four measured pages (D9 open) |
| INP / CLS, follow-up PERF | ≤ 200 ms / ≤ 0.1 | TBT proxy 42–57 ms; CLS 0.000–0.029 unchanged | 36–41 ms; 0.000–0.029 | proxy pass / pass |

Lantern's simulated LCP is higher than a replication with real CDP throttling on the same pages before the fixes (`lighthouse/diagnostics/lcp-probe.json`: `/en` 2.11 s, `/de` 2.92 s, `/en/integrations` 2.79 s, hub 2.36 s, pricing 1.89 s, article 1.74 s, login 1.40 s); both methods rank the same causes (fonts, document size, hydration). Field data (CrUX) does not exist for a pre-release site; the lab numbers are the release evidence.

### 6.5 Visual regression

`apps/web/e2e/visual.spec.ts` (project `visual` in `playwright.config.ts`, `snapshotPathTemplate` → `apps/web/e2e/__screenshots__/<snapshot>-visual-<platform>.png`): `home`, `pricing`, `knowledge-hub`, `article-consent-mode-v2-guide`, `login` (anonymous) and `app-overview` (stored owner session) at 375 and 1440 px, reduced motion, first 2 500 px, `maxDiffPixelRatio` 0.01, masks for timestamps, "Measured …" lines, progress bars and the persisted Track AI transcript. Baselines generated against the `85fe3b7` production build (`reports/e2e/visual-update-final.log`, 12 passed) and verified four times (`reports/e2e/visual-verify-final-{1,2,3,4}.log`, 12 passed each). The baselines are Windows-only (`-win32`, bundled Chromium of `@playwright/test` 1.62); CI does not run Playwright yet (defect D14). Procedure and update rules: `apps/web/e2e/README.md`.

### 6.6 Living AI Core budget (docs/15 §4)

Measured: Lighthouse on `/app` with the panel docked — accessibility 100, best practices 100, performance 72 → 89, TBT 397 → 57 ms (§6.3); the e2e specs prove the static tier under reduced motion and motion `off` and the viewport-high shell with 250 messages. **Not produced** in the first pass (defect D10): the comparison "mobile Lighthouse median within 3 points of the static panel", the long-task check while idle, the 30-minute soak for memory/listeners, the qualitative visual comparison and the cross-browser matrix — these need a browser session per configuration and were outside every QA task of 2026-09-05.

Follow-up 2026-09-05 (tasks E1, E2, E3; supplement §9 "Verbindliche Abnahmekriterien", docs/15 §4), all on build `JkuZkqiqEn0HgN4FIDCyT` unless stated:

| Criterion (supplement §9) | Result | Evidence |
| --- | --- | --- |
| Reduced motion / setting `off`: no continuous animation frames | **pass** — 0 `requestAnimationFrame` callbacks (0 requested, 0 fired) in 10 s windows for `prefers-reduced-motion: reduce`, for `off` after the header toggle on the same page and for `off` after a reload; `data-tier="static"`, no `canvas.lac-gl`, 0 running animations inside `.lac`, `.lac-blob > i` `animation-name: none`; the SSR HTML carries `data-tier="static"` and no `<canvas>`; the WebGL2 context created before the toggle was released (`webglcontextlost` 1); preference restored to `system` | `docs/qa/2026-09-05/followup/living-core/motion-off.json`, `motion.log`, `summary.md` §(d) |
| Static-vs-animated: median of 3 mobile Lighthouse runs at most 3 points below the static panel | **FAIL (−6 points)** — docked variant (mobile preset with the screen emulation widened to 1280 × 800 @ DPR 2 so that the panel is open and the core mounted on the `webgl` tier, verified per group in `lighthouse/tier-checks.json`): setting `off` (static) 75 / 79 / 77 → median **77**; `system` (webgl) 70 / 71 / 71 → **71**; `full` (webgl) 76 / 71 / 70 → **71**; TBT +209 / +198 ms (430 / 419 vs 221 ms), LCP +81 / +83 ms, CLS +0.001, main-thread work 2.15 / 2.17 vs 1.70 s, script boot-up 1.15 s vs 824 ms. Control on the default 412 px phone emulation (panel closed, core not mounted): 83 vs 83, no delta. The `full` / phone group (3 runs) is missing because the runner was stopped | `docs/qa/2026-09-05/followup/living-core/lighthouse/summary.md` + `summary.json`, 15 raw `app--{off,system,full}--docked--run{1,2,3}.report.json` and `app--{off,system}--phone--run{1,2,3}.report.json`, `run.log`; defect D17 |
| Idle animation causes no long tasks > 50 ms; scrolling, typing and buttons stay smooth | **not run** — the run was stopped before the check started; harness ready: `apps/web/scripts/qa/living-core-budget.mjs --mode longtasks` (120 s idle + 120 s scroll / type with a `PerformanceObserver` `longtask` and Chrome trace attribution), smoke-tested in `tier` mode (`tier-smoke-1440.json`: `webgl` tier on the headless shell with `--use-angle=d3d11`) | `docs/qa/2026-09-05/followup/living-core/summary.md` §(a), `summary-notes.md`; D10 open |
| ≥ 30-minute run with state changes: no growth of memory, listeners, canvases or WebGL resources | **not run** — same reason; `--mode soak` implements the 20 s state cycle with the provider stubbed at the browser's `fetch` boundary and CDP heap / DOM / listener samples every 60 s (untested end-to-end) | `summary.md` §(b); D10 open |
| Fallbacks: disabled, unavailable or lost WebGL context → CSS / static tier, chat stays usable | **pass in Chromium 151 and WebKit 26.5** (E2, `/app` at 1440 × 900): WebGL2 available → `webgl` (canvas opacity 1; Chromium 54 draws / 2 s at ~61 rAF/s with a 1 : 1 backing store; WebKit 37 draws / 2 s at ~26 rAF/s, descriptor DPR 2 capped to 1.5 × → 599 × 336 px for 399 × 224 CSS px); `prefers-reduced-motion` → `static` (0 draws, 0 running animations, no canvas); `getContext("webgl2")` = `null` → `css` with `lac-drift-a/b/c` running and the composer focusable and typable; `WEBGL_lose_context` → `css`, canvas released; dark theme → same tier. WebKit has no `requestIdleCallback` (the `setTimeout` fallback runs). 0 console errors in Chromium; WebKit logs only the viewport-meta message D21 | `docs/qa/2026-09-05/followup/browsers/runs/xb-lac-{chromium,webkit}/lac/*.json` + `*.webp`, `browsers/summary.md` §4, §6; `engine-smoke.json` |
| CLS = 0, no scroll-position change, composer and last message visible with the on-screen keyboard open | **defect found and fixed** (E3, D16): with the sheet open and the composer focused, shrinking the viewport 375 × 812 → 375 × 430 kept the transcript's `scrollTop` (21 352) while its `clientHeight` fell 600 → 218, so the last message sat at 631–707 px, below the 430 px viewport (the composer at 354–418 stayed visible). Fix in `apps/web/src/components/chat/assistant-chat.tsx` (`AssistantMessages`): a `ResizeObserver` on the scroll container re-pins `scrollTop = scrollHeight` when the reader is at the end and syncs the windowing viewport. After (build `2p2BhUEe9szAqx7yJQBwy`): last message 249–325 inside the list (123–341) above the focused composer, sheet 430 px, document fits; layout shifts: one resize entry 0.1839 with `hadRecentInput: true` (excluded from CLS), none in the 1.5 s idle window with the core animating. Permanent spec `app.spec.ts` "mobile on-screen keyboard". Limit: headless Chromium cannot shrink the visual viewport alone (`cdp-viewport-probe.log`), so the layout viewport is resized and `100dvh` vs the `visualViewport` listener are not distinguished — a device-lab run stays owed | `docs/qa/2026-09-05/followup/keyboard/README.md`, `before-fix/keyboard-metrics.json` + `keyboard-after.webp`, `after-fix/keyboard-metrics.json` + `keyboard-after.webp`, `e2e-keyboard-before-fix.log` (failed: expected ≤ 342, received 707), `e2e-keyboard-after-fix.log` (passed) |
| Chat, setup state, running jobs and scroll position survive a route change; site switch confirms the context and never mixes data | **pass** for transcript, scroll position, draft and panel geometry (250 fixture messages, `scrollTop` 10 616 → 10 616, draft unchanged, panel rect 1040 / 56 / 400 × 844 unchanged across Events → Command Center with the fixture parameter gone from the URL) and for the site switch (context line `Track AI now works on E2E switch site.`, header `Workspace switched to E2E switch site.`, `data-total` 0 and no fixture message for the other site, 250 restored on switching back); no code change was needed. **Not exercised**: a turn in flight (job state) — no AI provider in the lab and the fixture carries no activities; the mobile sheet closes on navigation by design | `docs/qa/2026-09-05/followup/state/README.md`, `after-fix/route-change.json`, `after-fix/site-switch.json`, `e2e-state-after-fix.log` (2 passed); permanent specs `app.spec.ts` "Track AI state across routes and sites" |
| Light and dark mode, long chats, narrow viewports | dark theme: same tier in both engines (above); dark-theme contrast of the 204 dashboard nodes 204 / 204 (§6.2); long chat: 250-message shell spec (§7) and the state specs; narrow viewports: bottom sheet viewport-high with the composer inside the viewport on "iPhone 14" (652 ≤ 664 px) and "Pixel 7" (827 ≤ 839 px), no horizontal scroll, axe 0 violations | `browsers/runs/xb-mobile-webkit-iphone14/mobile/*.json`, `xb-mobile-chromium-pixel7/mobile/*.json`, `contrast/results-dark.json` |
| Safari/iOS, Chrome/Android, Safari, Chrome, Firefox, Edge | **Chromium and WebKit only** (desktop + Playwright device emulation): Chromium 40 / 40 functional, 12 / 12 visual, 5 / 5 core cases, 3 / 3 mobile; WebKit 38 / 39 functional (the one failure is the sign-in spec's hydration race D20), 9 / 12 visual against the Chromium baselines (three at ratio 0.02 from glyph rasterisation and 1–3 px metric shifts, no structural difference), 5 / 5 core cases with the D21 console message, 2 / 3 mobile (the `/app` case fails only on D21). WebKit needed a harness-side hook that strips the build-time `upgrade-insecure-requests` directive (D18); Firefox cannot start on this machine (D19); real iOS / Android devices, Safari on macOS and Edge were not available / not run | `docs/qa/2026-09-05/followup/browsers/summary.md` (§0 matrix, §3 findings B1–B7, §6 tables), `matrix-tables.md`, `runs/**` |
| 200 % zoom, visual comparison ("ruhig, lebendig, hochwertig") | **not run** (no zoom state in any task; no user study) | D10 open |
| Headless-shell WebGL capability (method note) | the headless shell creates a strict WebGL2 context under the core's attributes both with SwiftShader (default) and with the Intel Iris Xe GPU via `--use-angle=d3d11` (used for every E1 measurement); `failIfMajorPerformanceCaveat` does not reject SwiftShader in this Chromium, so the "software-only → CSS tier" fallback could not be demonstrated here | `docs/qa/2026-09-05/followup/living-core/webgl-probe.json`, `apps/web/scripts/qa/living-core-webgl-probe.mjs` |

Follow-up 2 (2026-09-05 evening, tasks FX and MS; docs/15 §2 "Tier selection: timing and device rule", §4 table), all on the rebuilt build `9ZJAGzR3pMhmuGkmeGTk8` served on port 3016 with the stored owner session, headless shell with `--use-angle=d3d11`; evidence `docs/qa/2026-09-05/followup2/living-core/` (`README.md` indexes the artifacts, `summary.md` is generated from the JSON files by `living-core-report.mjs` and ends with the hand-written `summary-notes.md`):

| Criterion (supplement §9) | Result | Evidence |
| --- | --- | --- |
| Static-vs-animated: median of 3 mobile Lighthouse runs at most 3 points below the static panel | **pass (−1 point)** after the D17 fix — docked variant, 3 runs each: `off` (tier `static`) 82 / 79 / 81 → **81**; `system` (tier `css`: the mobile emulation's coarse pointer makes the device rule keep `system` on the CSS tier) 80 / 81 / 80 → **80**; `full` (tier `webgl`, upgrade observed 3 105 ms after the load event) 80 / 80 / 80 → **80**. TBT 107 / 99 / 95 ms (−8 / −12 ms), CLS 0.008 all, main-thread work 1.45 / 1.41 / 1.41 s, script boot-up 675 / 664 / 638 ms; simulated LCP 4.69 / 5.00 / 5.01 s (+315 / +320 ms — same LCP element in all 9 runs, FCP identical, observed render delay not higher: open observation on the Lantern value, cause not isolated). Not comparable in absolute terms with the first follow-up (static baseline 77 → 81 from the D9 changes in the same build) | `followup2/living-core/lighthouse/summary.md`, `summary.json`, `tier-checks.json`, `lcp-elements.json`, 9 raw `app--{off,system,full}--docked--run{1,2,3}.report.json`, `run.log`; defect D17 (fixed) |
| Idle animation causes no long tasks > 50 ms; scrolling, typing and buttons stay smooth | **pass** on this machine — desktop viewport, tier `webgl` confirmed before the window: **0** `longtask` entries in 120.0 s idle (7 201 rAF callbacks, p95 0.3 ms, 0 > 16 ms), 60.1 s scrolling (253 wheel events) and 60.6 s typing (1 280 characters, rAF max 0.6 ms); Chrome trace over the 241 s: 158 971 main-thread tasks, none ≥ 32 ms, longest 18.5 ms (input handling), `FireAnimationFrame` mean 0.22 ms | `followup2/living-core/longtasks-d3d11.json`, `longtasks-d3d11.trace-summary.json`, `longtasks.log` |
| ≥ 30-minute run with state changes: no growth of memory, listeners, canvases or WebGL resources | **pass — bounded, proportional to the transcript**: 30.0 min, 89 steps / 44 stubbed turns (every state except `approval_required`); GC'd heap 7.58 MB (minute 5, 37 messages) → 9.21 MB (minute 30, 99 messages) = +1.63 MB for +62 kept messages with a falling per-message increment; Chrome `JSEventListeners` 390 → 391, in-page listener ledger 327 constant from minute 5; 1 canvas / 1 WebGL2 context / 0 lost, observer counts constant. One 83 ms long task at 3.01 s after navigation (the WebGL upgrade itself) before the first sample — observation, outside every idle window | `followup2/living-core/soak.json`, `soak-samples.csv`, `soak.log` |
| Reduced motion / setting `off`: no continuous animation frames | **pass** (re-confirmed on the rebuilt bundle) — 0 rAF in 3 × 10 s windows (reduce, toggle to `off` from the `webgl` tier → `static` 75 ms later with the context released, reload with persisted `off`), SSR static, preference restored to `system` (gate re-armed, tier `webgl` again) | `followup2/living-core/motion-off.json`, `motion.log` |
| Hidden tab in a browser, real devices (Safari/iOS, Chrome/Android, Edge), Firefox, 200 % zoom, visual comparison, INP | **not run** (unchanged: D10 environment-bound items, D16 device lab, D19) | — |

## 7. Test and production build output

All outputs under `docs/qa/2026-09-05/reports/` (`run-gates.sh`, `_gates.log` with timestamps; `NO_COLOR=1 CI=1`, turbo `--force` so no cached log is replayed; no server started). Host: Windows 11, Node 24.18.0, pnpm 11.21.0, HEAD `85fe3b7` + working tree.

| Gate | Command | Result | Output |
| --- | --- | --- | --- |
| Typecheck | `pnpm typecheck --force` | **exit 0**, 17 / 17 tasks | `reports/typecheck.txt` |
| Lint | `pnpm lint --force` | **exit 1**: 16 / 17 tasks clean; `@track-site/web` reports 14 errors (`no-console`, `no-empty`, `no-unused-vars`, `no-unused-expressions`), **all in the untracked QA scripts** `apps/web/scripts/qa/{crawl,lighthouse,responsive-a11y-sweep}.mjs` | `reports/lint.txt` |
| Lint without the QA scripts | `pnpm exec eslint . --ignore-pattern "scripts/qa/**"` in `apps/web` | **exit 0** (application, e2e and F1 code lint clean) | `reports/lint-excluding-qa-scripts.txt` |
| Unit tests | `pnpm test --force` | **exit 0**, 15 / 15 tasks, 0 cached; **823 tests passed**: catalog 20, core 35, events 8, policy 17, config 7, analytics 2, connectors 8, queue 4, sdk 12, collector 24, worker 16, ai 170, web 500 (58 files) | `reports/test.txt` |
| Integration tests, parallel | `pnpm test:integration` | **exit 1**: PostgreSQL `deadlock detected` (40P01) inside `packages/db/src/testing/global-setup.ts` when db, worker and web migrate the shared `tracksite_test` database at the same time under turbo's default concurrency; web then reported "No test files found" | `reports/test-integration-run1-parallel.txt` (defect D7) |
| Integration tests, serial | `pnpm test:integration --concurrency=1` | **exit 0**, 7 / 7 tasks; **22 tests passed**: db 8 (RLS, event store), queue 3, analytics 2, worker 6 (pipeline), web 3 (usage ledger); collector 0 (`--passWithNoTests`) | `reports/test-integration.txt` |
| Contract tests | `pnpm test:contract` | **exit 0**, **63 tests in 7 files** against the mock vendors | `reports/test-contract.txt` |
| Production build | `pnpm --filter @track-site/web build` | **exit 0**, Next.js 16.3.4 (Turbopack), compiled in 3.8 s, **715 / 715 static pages** in 18.2 s, `BUILD_ID` `8k-9hBUufCX88fLKrM1Yp`; pre-existing warning `no CONFIG_SIGNING_PUBLIC_KEY set; the built tracker will reject every config` (the SDK build reads the shell environment, not `.env`; see `docs/qa/2026-09-05/README.md`) | `reports/build.txt`; earlier builds of the same tree: `recheck/build.log` (`UANQbZ2DkEqCtTt7EriZY`), P0 logs referenced in `docs/qa/2026-09-05/README.md` |
| SEO gate | `SEO_BASE_URL=http://localhost:3003 pnpm seo:check` (S3) | **exit 1**: 202 problems on 336 pages — 102 × "expected one `<title>`" (the checker counts the accessible `<title>` elements of the inline SVG diagrams: `/en` has 1 document title + 2 SVG titles, `/en/features` 9), 96 × "missing BlogPosting JSON-LD" (reference/tutorial articles carry `TechArticle` by design, `knowledge-article.test.ts`), 4 × description length 171–175 > 170 (fr: `event-taxonomy-standard-events`, `dsar-deletion-tracking-data`, `tcf-2-2-gpp-gpc`; it: `kill-switch-incident-playbook`); no `lang`, canonical, hreflang, h1, robots, sitemap or feed problem | `seo/seo-check.txt` (defects D4, D5) |
| SEO / schema / broken-link crawl | `node apps/web/scripts/qa/crawl.mjs` (S3, production build on :3003) | 498 pages (468 public + 30 dashboard): 497 × 200, 1 × 307; canonical, description and hreflang ×7 on 468 / 468; 912 JSON-LD blocks, 6 with errors (`/docs` `TechArticle` without `datePublished` / `author`, defect D3); 186 broken `og:image` targets (defect D1); 1 page with two `h1` (D2a); 14 titles > 60 characters and 62 article headlines > 110 characters (warnings, D4) | `seo/summary.md`, `seo/crawl.json` |
| Knowledge content | `pnpm --filter @track-site/web knowledge:validate` | **exit 0**: 180 files, 30 groups, six locales, learning paths valid, 0 front-matter changes, 0 `/blog` links | `seo/knowledge-validate.txt` |
| i18n parity | `node apps/web/scripts/i18n-parity.mjs --strict` (S3) | **exit 0**, 0 gaps in every section | `docs/i18n-parity-report.md`, copy in `seo/` |
| e2e (Playwright, chromium project) | `pnpm --filter @track-site/web test:e2e` with `--grep-invert "phase 6\|soak"` (phase 6 close-out, same source tree before F1) | run 2: **28 passed**; run 4: **28 passed**; run 3: 27 passed, 1 failed — "prefers-reduced-motion renders the static tier" found `data-pref="off"` because the motion-preference test had just persisted `off` for the seeded owner (test-order dependency, defect D11); run 1 (35 tests incl. a temporary `phase6-verify.tmp.spec.ts`, since removed): 30 passed, 5 failed in the temporary spec | `reports/e2e/e2e-run{1,2,3,4}.log` (base URL not recorded in the logs; Playwright default `http://localhost:3000`) |
| e2e, new F1 specs | `npx playwright test --project=chromium --no-deps -g "responsive header\|keyboard focus in the header\|responsive layout"` on :3006 | **12 passed** | `recheck/e2e-new-specs.log` |
| Visual regression | `playwright test --project=visual --no-deps` on :3005 | 12 passed × 4 verify runs | `reports/e2e/visual-verify-final-{1..4}.log` |

The full e2e suite was not re-run inside this report task (no server may be started here); the F1 fixes changed only layout/CSS, fonts and the two e2e spec files whose new tests passed on the rebuilt server.

## 8. Open points

Only items that need real external credentials, legal texts to be approved, or third-party decisions (supplement §11, point 8). Everything else is done or listed as a defect with an owner in §9.

| # | Open point | Needed for | Owner | Evidence / where it is read |
| --- | --- | --- | --- | --- |
| O1 | Mail transport and vault on the Fly worker: `RESEND_API_KEY` (or `SMTP_URL`) + `MAIL_FROM` + `HOST_APP`, and the worker `MASTER_KEY` / KMS for encrypted webhook and Slack URLs | Alerts & Incident Mode e-mail / webhook / Slack delivery (`alerts` job); without them the event records `delivery[channel].error` and nothing is sent | owner (Fly secrets) | `docs/07-deployment-runbook.md` §Worker jobs, `apps/worker/src/env.ts`; the web app's own mail works (`/api/health` `mail: resend`, `mailDomain.status: sending_only_key`) |
| O2 | Stripe end-to-end test purchase on the live account (checkout → webhook → entitlement → portal) | proof of the billing loop with real money; the six prices verify (`billing: ok`), the webhook rejects unsigned calls (400) | owner (live Stripe) | `IMPLEMENTATION_STATUS.md` Stripe row, `reports/health-www-track-site.json` |
| O3 | Vendor platform apps: Google Ads developer token + OAuth client, LinkedIn app, Login with Amazon app, X developer app | OAuth "Connect" for those destinations | owner / vendor approval | `IMPLEMENTATION_STATUS.md` external blockers, `docs/05-connector-credential-matrix.md` |
| O4 | Legal review and approval of the imprint / privacy / terms texts and the operator identity (`LEGAL_*`); the live imprint renders an operator identity today (`https://www.track.site/en/imprint` names a Hong Kong Limited company), the wording in six languages has not been approved by counsel | publication of the legal pages as binding texts | owner / legal | `apps/web/src/lib/legal-copy/*`, `docs/07` §Vercel (`LEGAL_*`) |
| O5 | First real test event per destination on real vendor accounts and the shop integrations against one real Shopify, WooCommerce and Shopware installation each | production sign-off of connectors and commerce sources | owner / vendor accounts | `docs/09-integrations-handover.md` §7, `docs/10-commerce-integrations.md` checklist |
| O6 | `CONFIG_SIGNING_PUBLIC_KEY` present in the Vercel build environment | a tracker that verifies signed configs in production (the local builds in this pack embed no key and fail closed) | owner (Vercel env) | `docs/07` §Vercel env list; build warning in `reports/build.txt` |

Done since docs/11 §6 was written (no longer open): the three Stripe owner actions — yearly prices 190 / 900 / 1 800 €, product/plan name Pro and `STRIPE_PRICE_PRO_*` env names — are live (`billing: ok`, `deprecated: []`); the OpenAI key is set (`ai: ok`, three models available).

## 9. Defects and owed evidence (with owner)

| # | Defect | Severity | Owner | Evidence | Fix hint |
| --- | --- | --- | --- | --- | --- |
| D1 | `og:image` / `twitter:image` of the Tracking Knowledge hub and of all 180 articles answer **404**: the pages build the URL `…/opengraph-image` (`tracking-knowledge/page.tsx` line 23, `[slug]/page.tsx` line 40) while Next.js emits the metadata routes under dynamic segments with a hash suffix (`opengraph-image-1vcy9h`, `opengraph-image-qnehfz`); verified on the production build: `/en/tracking-knowledge/consent-mode-v2-guide/opengraph-image` → 404, `…/opengraph-image-qnehfz` → 200 `image/png`; `/en/opengraph-image` (static segment) → 200 | **critical** (§11 "Social Card" per language version fails; 186 broken targets in the crawl) | web / marketing | `seo/summary.md` "Broken links and images", `seo/crawl.json` | fixed 2026-09-05: the renderers became plain modules (`card.tsx`) served by stable route handlers `card.png/route.tsx` with `generateStaticParams`; page metadata and JSON-LD reference `…/tracking-knowledge[/<slug>]/card.png` (200 image/png, localized alt); e2e and the SEO checker follow |
| D2a | `/app/destinations` renders two `h1` elements | moderate | dashboard | `seo/summary.md` dashboard table | fixed 2026-09-05: the second `h1` came from the streaming skeleton `apps/web/src/app/app/destinations/loading.tsx` (visible in the raw HTML next to the streamed page); the skeleton now renders a presentational `<p>` |
| D2b | `/app/billing` links to the unprefixed `/contact?topic=enterprise` (one redirect hop) | minor | dashboard | `seo/summary.md` "Links answered via exactly one redirect" | fixed 2026-09-05: `/app/billing` links to `/<locale>/contact?topic=enterprise` via `useLocale()` |
| D3 | `/[locale]/docs` `TechArticle` JSON-LD lacks `datePublished` and `author` (6 blocks) | minor | marketing | `seo/summary.md` "Schema errors" | add the fields or use `WebPage` |
| D4 | Content limits: 4 fr/it article descriptions are 171–175 characters (checker limit 170), 14 titles > 60 characters, 62 article headlines > 110 characters (Google truncates) | minor | content / localization | `seo/seo-check.txt`, `seo/summary.md` "Titles longer than 60 characters", "Schema warnings" | shorten in the front matter |
| D5 | `apps/web/scripts/seo-check.ts` is stale for the redesign: counts SVG `<title>` elements as document titles and requires `BlogPosting` although reference/tutorial articles carry `TechArticle` → `pnpm seo:check` fails with 198 false positives | major (gate) | web QA tooling | `seo/seo-check.txt` | fixed 2026-09-05: decoded-length checks, `<head>`-scoped title count, BlogPosting or TechArticle, `card.png` rule — `pnpm seo:check` exit 0 (`reports/final/seo-check-after-d1-d5.txt`) |
| D6 | ~~`apps/web/scripts/qa/{crawl,lighthouse,responsive-a11y-sweep}.mjs` (untracked) fail `eslint` (14 errors) → `pnpm lint` fails~~ **fixed in the final verification (§11)**: progress lines go through a local `stdout()` helper (`process.stdout.write`, as in `scripts/i18n-parity.mjs`), the empty `catch` carries a comment, the comma expression became two statements, the unused `here` / `lhr` bindings were removed or prefixed | closed | web QA tooling | `reports/final/lint.txt` (exit 0), `reports/final/lint-before-fix.txt` | — |
| D7 | `pnpm test:integration` deadlocks when db, worker and web run their `global-setup.ts` migration of `tracksite_test` concurrently | major (gate; CI runs the same command) | platform / db | `reports/test-integration-run1-parallel.txt` | fixed 2026-09-05: root script runs `turbo run test:integration --concurrency=1` |
| D8 | `docs/redirects-blog-to-tracking-knowledge.md` still lists 2 active locales (generated 2026-09-03); the six-locale matrix lives in the evidence pack | minor (docs) | web | §4 | fixed 2026-09-05: matrix regenerated for six locales (224 rows) |
| D9 | Mobile performance 87–94 and LCP 2.92–3.99 s remain below the targets (≈ 95, ≤ 2.5 s). **Follow-up PERF 2026-09-05 (reduced, still open)**: changes C1–C11 (§6.3.1) took the message catalog, the header copy, the tariff catalogue, the integrations catalogue and the demo out of the critical path — medians of 3: performance 89 / 89 / 92 / 94 → **91 / 91 / 95 / 92**, LCP 3.63 / 3.64 / 3.17 / 3.02 → **3.45 / 3.50 / 2.86 / 3.35 s**, FCP −0.30 s, HTML transfer −52 to −55 KB, route chunks −4.8 to −14.6 KB gzip; a11y / BP / SEO 100, CLS unchanged. Root cause of the remaining gap: the simulated LCP graph on localhost contains the 131.2 KB gzip Next.js/React runtime, 89.6 KB of fonts and the 21.6 KB site-wide stylesheet, all finished before the first paint (`followup/perf/summary.md` §5) | major (target) — reduced | web performance | §6.3.1, §6.4, `docs/qa/2026-09-05/followup/perf/summary.md`, `lighthouse-before/`, `lighthouse-after/`, `bundle-attribution-{before,after}.md`, `lcp-graph-after.md` | remaining levers (`summary.md` §4/§8): `sideEffects: false` + sub-path exports for `packages/ui` (≈ 5–6 KB gzip), replace next-intl's client navigation on the marketing site to drop the ICU runtime (12 KB gzip), a single responsive rendering of the comparison matrix, self-hosted font subsetting per page, or accept the lab floor and judge by field data (CrUX) after launch |
| D10 | Living AI Core budget evidence of docs/15 §4 (static-panel comparison, idle long-task check, 30-minute soak, cross-browser matrix) not produced; no Firefox / WebKit run anywhere in the pack. **Follow-up E1 / E2 2026-09-05 (partly produced)**: reduced-motion / `off` check complete and passing; static-panel Lighthouse comparison produced (15 of 18 reports) and **failing** the ≤ 3-point criterion → D17; tier fallbacks and frame rate verified in Chromium and WebKit; cross-browser matrix produced for Chromium and WebKit (Firefox n/a → D19, WebKit through a hook → D18). **Still not run**: idle long-task check (a), 30-minute soak (b) — the E1 run was stopped before they started; the harness `apps/web/scripts/qa/living-core-budget.mjs` (modes `longtasks`, `soak`, `motion`, `tier`) and `living-core-report.mjs` are in the working tree; 200 % zoom, the qualitative visual comparison and real Safari/iOS, Chrome/Android, Edge. **Follow-up 2 MS 2026-09-05 evening (produced)** on build `9ZJAGzR3pMhmuGkmeGTk8`, port 3016, headless shell with `--use-angle=d3d11`: (a) idle long-task check — desktop viewport, tier `webgl` confirmed via `data-tier` before the window: **0** `longtask` entries in 120 s idle, 60 s scrolling (253 wheel events) and 60 s typing (1 280 characters); rAF callback p95 0.3 ms, max 0.6 ms; the Chrome trace over the same 241 s shows 158 971 main-thread tasks, none ≥ 32 ms, longest 18.5 ms (`focusin` on the composer click) — the animation's own per-frame cost is `FireAnimationFrame` mean 0.22 ms; (b) 30-minute soak — 89 steps / 44 stubbed turns through every state except `approval_required`, GC'd heap 7.58 MB (minute 5) → 9.21 MB (minute 30) for 37 → 99 transcript messages (+1.63 MB, the per-message increment falling 58 → 16 → 19 KB), Chrome `JSEventListeners` 390 → 391, in-page listener ledger 327 constant from minute 5, exactly 1 canvas / 1 WebGL2 context / 0 lost, IO / RO / MO counts constant — growth bounded and proportional to the transcript, not to time or state changes; (c) static-panel Lighthouse comparison re-run → D17 fixed; (d) reduced motion / `off` re-confirmed. Observation: one 83 ms long task at 3.01 s after navigation in the soak page coincides with the WebGL upgrade (renderer import + shader compile + first frame) — outside the load window and every measured idle window, recorded, not a failure. **Still not run**: hidden-tab frames in a real browser, 200 % zoom, the qualitative visual comparison, INP, real Safari/iOS, Chrome/Android, Edge, Firefox (D19) | ~~moderate (owed evidence)~~ **reduced 2026-09-05 (follow-up 2)** to the environment-bound items (devices, Firefox / Edge, zoom, visual comparison) | QA | §6.6, `docs/qa/2026-09-05/followup2/living-core/summary.md` (generated) + `summary-notes.md`, `longtasks-d3d11.json`, `longtasks-d3d11.trace-summary.json`, `longtasks.log`, `soak.json`, `soak-samples.csv`, `soak.log`, `motion-off.json`, `motion.log`, `README.md`; first follow-up: `docs/qa/2026-09-05/followup/living-core/summary.md`, `summary-notes.md`, `docs/qa/2026-09-05/followup/browsers/summary.md` | device lab (Safari/iOS, Chrome/Android, Edge) for long tasks and the keyboard (D16), a Firefox machine (D19), a 200 % zoom pass and the visual comparison with users; reproduction from `apps/web`: `node scripts/qa/living-core-budget.mjs --mode longtasks --base http://localhost:<port> --out <dir>`, `--mode soak --soak-min 30`, `--mode motion`, then `node scripts/qa/living-core-report.mjs --dir <dir>` |
| D11 | e2e test-order dependency: the reduced-motion spec reads the seeded owner's persisted `ai_motion` preference, which another spec sets to `off`. Follow-up status: not reproduced in the four full Chromium runs of the follow-up (PERF 52 / 52, E2 40 / 40, E3 42 passed, §11.1), but the shared preference was toggled by failing runs of another engine while E2 ran (B7: left at `off` by the broken first WebKit run, restored by later passing runs) — the dependency is real whenever runs share the seeded owner; the sign-in spec's hydration race in WebKit (D20) is a second timing dependency of the same kind | minor (flaky) — open | web | `reports/e2e/e2e-run3.log`, `docs/qa/2026-09-05/followup/browsers/summary.md` §3 B7 | reset the preference in the spec or use a dedicated user; one e2e directory / user per concurrent run |
| D12 | `updateLocaleAction(locale: "en" \| "de")` in `apps/web/src/server/actions/settings.ts` keeps the two-locale signature; it is unused (the settings action validates `z.enum(ALL_LOCALES)`) | trivial (cleanup) | web | grep in this report | delete or retype |
| D13 | The F1 fixes and the QA tooling are **uncommitted** (35 modified files, 5 untracked paths) — the evidence in `recheck/` and `reports/` was produced from that working tree | blocking for release | integrator | `changed-files.md` §3 | fixed 2026-09-05: committed on `feat/ai-tag-manager-platform`, deployed from `main` |
| D14 | Visual baselines are `-win32` only and CI (`ci.yml`, ubuntu) runs no Playwright project; a Linux run writes its own baselines and fails once | minor (CI) | CI | `apps/web/e2e/README.md` | add a Playwright job with Linux baselines |
| D15 | axe "incomplete" `color-contrast` nodes (1 935 across 96 runs) unverified | ~~minor (owed check)~~ **fixed 2026-09-05 (E3)** | QA / design | `axe/summary.md`; `docs/qa/2026-09-05/followup/contrast/README.md`, `summary.md`, `results.json` (before: 1 879 pass / 48 FAIL / 8 not found), `results-after.json` (1 935 pass), `results-dark.json` (204 pass) | done: the review script `contrast-review.mjs` evaluated every node; 48 failing evaluations (38 nodes, 5 root causes) fixed in `packages/ui/src/styles/tokens.css` (stage accent tokens, dark / stage `--color-primary-soft`), `packages/ui/src/diagram.tsx`, `apps/web/src/components/marketing/pricing/event-definition.tsx`, `apps/web/src/components/app/data-quality/inbox.tsx`, `packages/ui/src/primitives/search.tsx` (§6.2); the 12 visual baselines still pass (`followup/state/e2e-visual.log`); not swept: marketing pages in dark mode |
| D16 | The mobile on-screen keyboard vs composer check (§11 "mobile Bildschirmtastatur verdeckt den Composer nicht") and the route-change persistence of chat/job state were not tested end-to-end in this pack (the shell is `100dvh` with the composer in the sheet; the store persists server-side) | ~~moderate (owed evidence)~~ **partly fixed 2026-09-05 (E3)**: keyboard defect found and fixed, route change and site switch proven; job state and a real device keyboard still owed | QA / web | `docs/qa/2026-09-05/followup/keyboard/README.md` (+ `before-fix/`, `after-fix/`, `e2e-keyboard-{before,after}-fix.log`, `cdp-viewport-probe.log`), `docs/qa/2026-09-05/followup/state/README.md` (+ `after-fix/route-change.json`, `after-fix/site-switch.json`, `e2e-state-after-fix.log`) | keyboard: the transcript kept its `scrollTop` while its height shrank, hiding the last message below the 430 px viewport → `ResizeObserver` re-pin in `apps/web/src/components/chat/assistant-chat.tsx` (§6.6); permanent specs in `apps/web/e2e/app.spec.ts` ("mobile on-screen keyboard", "Track AI state across routes and sites" — the site-switch spec creates and soft-deletes a second site directly in the local database because the starter plan allows one site and the product has no site deletion; residue: one soft-deleted row). Still owed: device-lab run with a real visual-viewport keyboard (headless Chromium cannot shrink the visual viewport alone), a route change during a real tool run (needs AI / vendor credentials) |
| D17 | **Living AI Core misses the static-panel Lighthouse criterion** (supplement §9: median of three mobile runs at most 3 points below the static chat version): with the panel docked and the core on the `webgl` tier the median is 71 for `system` and for `full` vs 77 for `off` (**−6 points**), TBT +209 / +198 ms (430 / 419 vs 221 ms), LCP +81 / +83 ms, main-thread work 2.15 / 2.17 vs 1.70 s, script boot-up 1.15 s vs 824 ms — the WebGL renderer chunk and its first frames run inside the load window under the 4 × CPU simulation. On the default phone emulation the panel is closed and the core not mounted (83 vs 83), so only the docked variant measures the core. **Fix 2026-09-05 (FX)**: new pure timing gate `apps/web/src/components/app/shell/living-ai-core/upgrade-gate.ts` — the WebGL renderer is requested only after `document.readyState === "complete"` (else the `load` event), ≥ 3 s since navigation start on the component's clock (`UPGRADE_MIN_ELAPSED_MS`, timer bridges the remainder and re-reads the clock) and a `requestIdleCallback` with a 4 s timeout (500 ms timer where the API is missing); the CSS tier always renders first; the first WebGL frame is drawn while the canvas is still transparent, the tier attribute flips one rAF later and the existing `.lac-gl` / `.lac-css` opacity transitions (`--lac-t` = 600 ms) cross-fade, so there is no visible jump; device rule in `tier.ts` (`isConstrainedDevice` / `webglPermitted`: coarse pointer, `saveData`, `deviceMemory ≤ 4`, `hardwareConcurrency ≤ 4` → CSS tier is final for `system`, WebGL only for an explicit `full`); documented in docs/15 §2, unit tests `upgrade-gate.test.ts` (6), `tier.test.ts`, `living-ai-core.dom.test.tsx`. **Re-measured 2026-09-05 (MS)** on build `9ZJAGzR3pMhmuGkmeGTk8`, same docked method, 3 runs each: `off` 82 / 79 / 81 → **81**, `system` 80 / 81 / 80 → **80**, `full` 80 / 80 / 80 → **80** → delta **−1 / −1 points** (target ≤ 3: pass); TBT 107 / 99 / 95 ms (−8 / −12 ms), CLS 0.008 all, main-thread work 1.45 / 1.41 / 1.41 s, script boot-up 675 / 664 / 638 ms. Tier under the mobile emulation (`tier-checks.json`): `off` → `static`; `system` → `css` — the emulation's coarse pointer makes the device rule keep `system` on CSS, so that group measures the CSS tier; `full` → `webgl`, the flip observed 3 105 ms after the load event. Open observation: the simulated LCP is +315 / +320 ms (4.69 → 5.00 / 5.01 s) although the LCP element is the same text paragraph in all 9 runs, FCP is identical (1.67–1.70 s) and the observed TTFB + render delay are not higher for the animated groups (`lcp-elements.json`) — the delta exists only in the Lantern-simulated value and was not isolated; recorded against "LCP darf sich nicht materiell verschlechtern", not as a failure. The absolute scores are not comparable with the first follow-up (the static baseline moved 77 → 81 with the D9 PERF changes in the same build); only the within-run deltas (−6 → −1) are the D17 evidence | ~~major (acceptance criterion)~~ **fixed 2026-09-05 (FX) and verified by measurement (MS)** | web / Living AI Core | §6.6, `docs/qa/2026-09-05/followup2/living-core/lighthouse/summary.md` + `summary.json`, `tier-checks.json`, `lcp-elements.json`, raw `app--{off,system,full}--docked--run{1,2,3}.report.json`, `run.log`; fix gates `docs/qa/2026-09-05/followup2/fx/{typecheck,eslint,vitest,build}.log`; before: `docs/qa/2026-09-05/followup/living-core/lighthouse/summary.md` | none for the criterion; optional: isolate the simulated-LCP delta (Lantern graph diff between an `off` and a `full` report), measure a `system` WebGL group on a fine-pointer emulation if a mobile-class WebGL figure is wanted (impossible under the mobile Lighthouse emulation by design of the device rule) |
| D18 | **WebKit cannot load a local production build over plain http**: `apps/web/next.config.ts` bakes `Content-Security-Policy: …; upgrade-insecure-requests` (+ HSTS) into `.next/routes-manifest.json` whenever `NODE_ENV === "production"`; WebKit applies the directive to `http://localhost` and upgrades every stylesheet, script, font, same-origin link and form post to https (SSL connect error, 19 failed requests per page) — pages render unstyled and never hydrate; Chromium and Firefox exempt localhost. Unmodified WebKit run: 20 of the 39 committed tests fail for this reason alone. `bypassCSP`, `127.0.0.1`, a runtime `NODE_ENV=development` and stripping HSTS alone do not help; stripping the directive does. Harmless on the https production origin. **Fix 2026-09-05 (FX)**: `apps/web/next.config.ts` now builds the CSP through the exported pure `buildContentSecurityPolicy({ production, secureOrigin, ingest, cdn })` and emits `upgrade-insecure-requests` only when `isSecureOrigin(process.env.HOST_MARKETING)` (scheme `https:`); `'unsafe-eval'` still follows `NODE_ENV`. Verified on build `9ZJAGzR3pMhmuGkmeGTk8` (root `.env` `HOST_MARKETING=http://localhost`): the `Content-Security-Policy` value in `.next/routes-manifest.json` carries the 11 other directives and no `upgrade-insecure-requests`; the 5 other headers incl. HSTS are unchanged (`fx/routes-manifest-csp.txt`); unit tests `apps/web/src/next.config.test.ts` (5: builder + the exported config's `headers()` under `HOST_MARKETING=http://localhost:3015` vs `https://track.site`). Limits: only the CSP directive is gated — `Strict-Transport-Security` is still emitted whenever `NODE_ENV=production` (browsers ignore HSTS on non-TLS responses; stripping HSTS alone changed nothing in the probes); if `HOST_MARKETING` is unset at build time the directive is omitted (docs/07 lists the variable as required). **Not yet re-verified in WebKit** — no WebKit project ran in follow-up 2 | ~~major (QA tooling)~~ **fixed in code 2026-09-05 (FX); WebKit re-run owed** | web QA tooling | `docs/qa/2026-09-05/followup2/fx/routes-manifest-csp.txt`, `fx/vitest.log`, `fx/build.log`; before: `docs/qa/2026-09-05/followup/browsers/webkit-csp-probe.json`, `webkit-header-strip-probe.json`, `routes-manifest-headers.txt`, `e2e-webkit-production-headers.log`, `server-3013-node-env-development-attempt.log`, `summary.md` §3 B1 | run the `webkit` project (`E2E_ENGINES=webkit`) against a local build **without** the header-strip hook of `browsers/make-tmp-specs.mjs` and confirm 0 CSP-caused failures; optionally gate HSTS the same way |
| D19 | **Firefox cannot start on this machine**: Playwright `firefox-1538` is installed at the expected revision (`playwright install firefox webkit` exit 0, nothing downloaded) but `browserType.launch` fails with `spawn UNKNOWN` — Windows cannot build the Side-by-Side activation context of `firefox.exe` (dependent assembly `mozglue` 1.0.0.0 not found although `mozglue.dll` is present and carries its manifest); the same class of failure keeps the full Playwright Chromium from starting here. No Firefox result exists anywhere in the pack; the 5 request-only tests pass without a browser | blocking for the Firefox column (environment) | CI / QA | `docs/qa/2026-09-05/followup/browsers/firefox-sxs.txt`, `engine-smoke.{json,log}`, `playwright-install.log`, `e2e-firefox.log` (5 passed, 38 × spawn UNKNOWN), `runs/xb-{visual,lac}-firefox/run.log` | run the `firefox` project on a machine with a working SxS installation or on the Linux CI runner (D14); on this machine `E2E_ENGINES=` or `E2E_ENGINES=webkit` keeps `pnpm test:e2e` free of launch failures. With Firefox present-but-broken and D18 unfixed, a plain `pnpm test:e2e` here now includes 38 Firefox launch failures and 20 WebKit failures unless `E2E_ENGINES` narrows the projects; several engine projects in parallel can also exceed better-auth's 3 sign-ins per 10 s |
| D20 | **`app.spec.ts` sign-in races hydration**: the spec fills the controlled e-mail / password inputs right after `page.goto("/en/login")` (load ≈ 355 ms) while React hydrates ≈ 476 ms after navigation and resets the controlled inputs; the form then submits empty ("Enter a valid e-mail address.", field `[invalid]`) and `waitForURL(/\/app/)` times out. Chromium passes only because it hydrates before `fill()` completes; WebKit through the D18 hook does not. Signing in works in WebKit once hydration is awaited. Same for `auth.setup.ts`. **Fix 2026-09-05 (FX)**: `waitForLoginHydration(page)` in `apps/web/e2e/auth.setup.ts` and `apps/web/e2e/app.spec.ts` waits (`page.waitForFunction`) until `<html>` and the login `<form>` carry React's `__reactProps$…` DOM key, then fills and asserts `toHaveValue` before submitting. The login form has no `data-hydrated` attribute and `login-form.tsx` was outside the FX scope, so the wait relies on React's internal key (stable since React 17, used by the devtools); a future React major that renames it makes the wait time out loudly rather than pass silently — the durable fix remains a `data-hydrated` attribute set by the form's client component. Validated by a jsdom probe (React 19 `hydrateRoot` in `startTransition` with a react-hook-form input: the key appears only after the hydration commit; a value typed before it is reset, one typed after it survives — `fx/d20-hydration-probe.txt`) and by the Chromium `setup` sign-in of §11.2; **not yet run in WebKit** | ~~minor (test robustness, D11-type)~~ **fixed 2026-09-05 (FX); WebKit confirmation owed** | web (e2e) | `docs/qa/2026-09-05/followup2/fx/d20-hydration-probe.txt`, `docs/qa/2026-09-05/followup2/reports/e2e-run1.log` (setup project); before: `docs/qa/2026-09-05/followup/browsers/webkit-hydration-probe.json`, `runs/xb-func-webkit/run.log` failure 1 | run the `webkit` project after D18 (same run); later replace the `__reactProps$` probe by a `data-hydrated` attribute on the form |
| D21 | WebKit logs `Viewport argument key "interactive-widget" not recognized and ignored.` as a console error on every dashboard page — the Chromium-only `interactive-widget=resizes-content` key of the dashboard viewport meta (`apps/web/src/app/app/layout.tsx` line 24). Function is unaffected, but the "no console errors" criterion fails in WebKit for the 5 Living AI Core cases and the mobile `/app` check; marketing pages are clean. **Fix 2026-09-05 (FX)**: the key left the server-rendered viewport meta (`viewport` export of `apps/web/src/app/app/layout.tsx` no longer sets `interactiveWidget`); a fixed inline head script (`INTERACTIVE_WIDGET_SCRIPT`, no user data) appends `interactive-widget=resizes-content` to the viewport meta only where `"virtualKeyboard" in navigator` (VirtualKeyboard API, Chromium ≥ 94) — synchronously when React streamed the meta before the script, otherwise at the next head mutation / `DOMContentLoaded`, i.e. before any user can open the keyboard; idempotent. Verified on build `9ZJAGzR3pMhmuGkmeGTk8`: the dashboard layout chunk carries the script and the only remaining `interactiveWidget` string under `.next/server` is Next's generic viewport resolver (`fx/d21-build-check.txt`); the script parsed and run in jsdom appends the key for a Chromium-like `navigator` (before and after a late meta, once) and leaves the meta untouched for a WebKit-like one (`fx/d21-inline-script-probe.txt`). Limits: `virtualKeyboard` is a proxy for Chromium-class engines — Chromium 94–107 would append the key and warn once (pre-2023 versions); **WebKit console and real Android / Safari checks not yet run** (D16 device lab) | ~~minor~~ **fixed in code 2026-09-05 (FX); WebKit console check owed** | dashboard | `docs/qa/2026-09-05/followup2/fx/d21-build-check.txt`, `fx/d21-inline-script-probe.txt`; before: `docs/qa/2026-09-05/followup/browsers/runs/xb-lac-webkit/lac/*.json` (`consoleErrors`), `runs/xb-mobile-webkit-iphone14/mobile/xb-mobile-webkit-iphone14--app-*.json` | confirm 0 console errors on `/app` in the WebKit run of D18; confirm the keyboard resize on a real Android device (D16 device lab) |
| D22 | The follow-up changes are **uncommitted**: 39 modified tracked files and 18 untracked paths (PERF C1–C11 incl. `apps/web/next.config.ts`, `apps/web/.gitignore`, `apps/web/src/app/marketing.css`, the pricing / demo islands; E2 `apps/web/playwright.config.ts`; E3 `assistant-chat.tsx`, `tokens.css`, `diagram.tsx`, `search.tsx`, `inbox.tsx`, `event-definition.tsx`, `apps/web/e2e/app.spec.ts`; the QA scripts under `apps/web/scripts/qa/` and `docs/qa/2026-09-05/followup/`). Every number in §6.3.1, §6.6 and §11.1 was produced from that working tree; `apps/web/.next` is the gate build of §11.1 and contains all of them | ~~blocking for release (as D13 was)~~ **fixed 2026-09-05**: committed as `caab018` on `feat/ai-tag-manager-platform` (`git log`) | integrator | `git status --short`, §11.1 | done; the dark-mode visual review of `--color-primary-soft` surfaces remains open as a note under D15 |
| D23 | The follow-up-2 changes are **uncommitted**: 14 modified tracked files (+601 / −144) and 4 untracked paths — FX: `apps/web/src/components/app/shell/living-ai-core/{upgrade-gate.ts,upgrade-gate.test.ts,tier.ts,tier.test.ts,living-ai-core.tsx,living-ai-core.dom.test.tsx,preference.ts,index.ts}`, `apps/web/next.config.ts` + `apps/web/src/next.config.test.ts`, `apps/web/src/app/app/layout.tsx`, `apps/web/e2e/{auth.setup.ts,app.spec.ts}`, `docs/15-living-ai-core.md`; MS: `apps/web/scripts/qa/living-core-{budget,lighthouse,report}.mjs` (one CLI semantic changed: `--interact-s N` now maps to N/2 s scroll + N/2 s typing; the report renderer still accepts the old `interact` JSON); the evidence `docs/qa/2026-09-05/followup2/`. Every number in §6.6 (follow-up 2), §9 D10 / D17 / D18 / D20 / D21 and §11.2 was produced from that working tree; `apps/web/.next` is the gate build of §11.2 | blocking for release (as D22 was) | integrator | `git status --short`, §11.2 | review and commit on `feat/ai-tag-manager-platform`; note the two documented rule choices of docs/15 §2 (`hardwareConcurrency ≤ 4` counts as constrained, which keeps 4-core desktops on the CSS tier for `system`; the gate's 3 s mark is measured on the component's clock, so an injected clock must share `performance.now()`'s time base) |

## 10. Definition of done (supplement §11) — status per item

| Item | Status | Evidence |
| --- | --- | --- |
| `Track` is the visible brand, `track.site` only as domain | pass | `apps/web/src/lib/brand-guard.test.ts` (in `pnpm test`), crawl titles "· Track", `Organization` JSON-LD name "Track", before/after screenshots |
| Header, mobile navigation, footer, CTA and forms work in all six languages | pass (layout, navigation, CTA); forms: contact/demo/support action accepts every locale (`IMPLEMENTATION_STATUS.md` localization row), submission not e2e-tested | sweep home/pricing ×6 at 6 widths, e2e home ×6 + switcher, `recheck/README.md` #4–#6 |
| Hero demo reacts to keyboard, touch and mouse and changes state | partial: keyboard tab stops on `/en` include the demo controls with visible focus (40 / 40); touch/mouse interaction not scripted | `axe/keyboard-summary.md`, `before-after/home-*` (different stream steps captured) |
| No demo action sends real events or productive requests | by design (local fixtures, `docs/12` §5); not separately measured with a network log | — |
| No invented testimonials, logos, numbers, results | pass: no `Review`/`AggregateRating` in 912 JSON-LD blocks; the recommended plan is "Recommended", not "most popular" (`catalog.test.ts`) | `seo/summary.md` |
| No nested link/button markup | pass: axe `nested-interactive` (wcag2a) clean in 120 runs | `axe/*.json` |
| Pricing: exact amounts, plan consistency, no "price not published", deterministic finder/calculator, honest localized notes | pass | §5 |
| "Blog" fully replaced by "Tracking Knowledge" in the visible product | pass: no `/blog/` in sitemaps/feeds, 0 internal `/blog` links, routes removed, header/footer show "Tracking Knowledge" (screenshots) | §4, `seo/knowledge-validate.txt` |
| All old blog URLs have direct correct 301 targets | pass (308 permanent, no chains) | §4 |
| ≥ 30 topics × EN/DE/FR/ES/IT/NL published | pass (30 × 6, all `published`) | §3 |
| Every language version: content, slug, metadata, canonical, hreflang, social card, schema | **fail on social cards** (D1); the rest passes | §3, D1 |
| No mixed languages / English fallback on localized pages | pass structurally (strict parity, `pick()` throws); not measured by a classifier | §3 |
| `<html lang>` matches the language on every route | pass (336 gate pages, e2e ×6) | `seo/seo-check.txt`, e2e |
| Language switch stays on the same page/article | pass | e2e "language switcher leads to the same page" |
| Search, filters, TOC, related articles, social cards work | partial: TOC / template / feedback covered by the article e2e; hub search and filters have no automated test in this pack; social cards fail (D1) | e2e run logs, D1 |
| Machine-readable parity report per `translationGroupId` | pass | `docs/i18n-parity-report.json`, validator (30 groups) |
| ≥ 200 chat messages: app stays exactly viewport-high, only the transcript scrolls | pass (250 messages) | e2e "keeps the shell exactly viewport-high with a 250-message conversation" |
| Chat and composer reachable on desktop, tablet, mobile | pass after F1 (primary action reachable in 144 / 144 runs; mobile bottom sheet e2e) | §6.1, e2e |
| Mobile on-screen keyboard does not cover the composer | **pass after the E3 fix** (emulated keyboard = viewport 375 × 812 → 375 × 430): composer 354–418 px and the last message 249–325 px stay inside the 430 px viewport, no idle layout shift; before the fix the last message was hidden below the viewport (the composer was visible). Real device keyboard not run (D16) | §6.6, `docs/qa/2026-09-05/followup/keyboard/README.md`, e2e "mobile on-screen keyboard" (§11.1: passed) |
| Chat and job state survive route changes | **pass** for chat state (transcript, scroll position, draft, panel geometry across a route change; site switch confirms the context and never shows the other site's transcript — permanent e2e, §11.1); job state (a turn in flight) not exercised — no AI provider in the lab (D16) | §6.6, `docs/qa/2026-09-05/followup/state/README.md` |
| No reasoning, prompts, secrets, raw PII or unredacted tool data in DOM/stream/transcript/log | by contract: allow-listed UI events with redaction (`packages/ai/src/ui-events.ts`, 170 ai tests); no runtime DOM audit in this pack | `reports/test.txt` |
| Every activity message equals a real job state | by contract (sentence keys bound to real run ids; `job.progress` only with real stage names) | `docs/11` §5 phase 6 |
| Off-topic / prompt-injection eval ≥ 50 variants without an unauthorised tool call | pass: 63-variant eval in `packages/ai` (commit `83a9c9e`), part of the 170 ai tests | `reports/test.txt` |
| Mutations without a valid action-bound approval token impossible | by design and unit-tested (`packages/ai`, confirm route); not re-verified live here | `docs/08-ai-and-secrets.md` |
| Cross-tenant, role, reconnect, timeout, double-click, replay tests green | partial: RLS integration tests (db 8), reconnectable turns without duplicate execution (`TurnRegistry` tests), webhook replay/signature tests; no dedicated e2e matrix in this pack | `reports/test-integration.txt`, `reports/test.txt` |
| All new dashboard modules show real states or honest empty states | pass: 30 dashboard URLs render (200, one `h1` except D2a) with the seeded data; worker-fed pages show "not measured" until the jobs ran | `seo/summary.md` dashboard table, `before-after/app-*` |
| Final evidence: lint, typecheck, unit, integration, e2e, a11y, SEO, schema, broken-link, visual regression, production build | see §7, §11 and §11.1 — lint passes after the D6 fix; the SEO gate passes after D5; integration passes serially (D7); the follow-up run repeats typecheck, lint, unit, build and the full e2e suite on the working tree with the follow-up changes (all green, §11.1) | §7, §11, §11.1 |
| Living AI Core acceptance criteria (supplement §9) | partial: reduced motion / `off`, fallbacks, frame rate, geometry, keyboard, route-change state pass; the static-panel Lighthouse comparison **fails** by 6 points (D17); idle long tasks, 30-minute soak, 200 % zoom, visual comparison and Firefox / Edge / real devices not run (D10, D19) | §6.6 |

## 11. Final verification of phase 7 (2026-09-05, after the F1 fixes)

One closing run of every gate on the working tree described in §1 (HEAD `85fe3b7` + F1 fixes + QA tooling), production build served on port 3007 with `AI_DEV_FIXTURES=1` and `HOST_MARKETING` / `HOST_APP` pointed at that port (better-auth only trusts the configured origin, so the Playwright `setup` sign-in would otherwise be rejected on any port but 3000). Outputs under `docs/qa/2026-09-05/reports/final/`.

| Gate | Command | Result | Output |
| --- | --- | --- | --- |
| Typecheck | `pnpm typecheck` | **exit 0**, 17 / 17 tasks (15 replayed from the turbo cache, `web` and `collector` executed) | `reports/final/typecheck.txt` |
| Lint, first run | `pnpm lint` | **exit 1**: the 14 errors of D6 in `apps/web/scripts/qa/{crawl,lighthouse,responsive-a11y-sweep}.mjs` | `reports/final/lint-before-fix.txt` |
| Lint, after the in-place fix | `pnpm lint` | **exit 0**, 17 / 17 tasks; `node --check` on the three scripts passes | `reports/final/lint.txt` |
| Unit tests | `pnpm test` | **exit 0**, 15 / 15 tasks, **823 tests passed** (web 500 in 58 files executed; the 14 package tasks replayed from the turbo cache with their counts: catalog 20, core 35, events 8, policy 17, config 7, analytics 2, connectors 8, queue 4, sdk 12, collector 24, worker 16, ai 170) | `reports/final/test.txt` |
| Production build | `pnpm --filter @track-site/web build` | **exit 0**, compiled in 4.1 s, **715 / 715 static pages** in 17.0 s, `BUILD_ID` `WYwzaImZB_f2TD_I3NFt-`; the known `CONFIG_SIGNING_PUBLIC_KEY` warning (O6) | `reports/final/build.txt` |
| e2e, full suite (setup + chromium + visual), run 1 | `E2E_BASE_URL=http://localhost:3007 pnpm --filter @track-site/web test:e2e` | 52 tests: **48 passed, 4 failed** — (a) `marketing.spec.ts` "Tracking Knowledge index …" fetched the JSON-LD publisher logo by its absolute URL, which carries the build-time `HOST_MARKETING` origin `http://localhost:3000` (`ECONNREFUSED`, not a product defect); (b) visual `home-375`, `home-1440`, `app-overview-375` differed from the baselines of 11:47–11:57, which predate the F1 fixes (font subset / size-adjusted fallback shifts the hero and the sections below it by a few pixels, the hero demo lays out by container query, the dashboard header budget at 375 px) — ratios 0.05 / 0.05 / >0.01 | `reports/final/e2e-run1-before-baseline-update.log` |
| Fixes | spec + baselines | (a) the spec now requests the logo by pathname from the server under test (`apps/web/e2e/marketing.spec.ts`); (b) the three baselines were regenerated **once** with `playwright test --project=visual --update-snapshots` after reviewing the diff images (`apps/web/e2e/__screenshots__/{home-375,home-1440,app-overview-375}-visual-win32.png`, the other nine files unchanged) | `reports/final/visual-update-snapshots.log` |
| e2e, full suite, run 2 | same command | **exit 0, 52 / 52 passed** (1 setup, 39 chromium incl. axe on the six home pages and the article template, 12 visual) | `reports/final/e2e-run2.log` |
| Visual regression, second consecutive pass | `playwright test --project=visual` | **13 / 13 passed** (setup + 12 snapshots) | `reports/final/visual-verify-run3.log` |
| Temporary specs | `ls apps/web/e2e/*.tmp.spec.ts` | none present | — |
| Evidence pack completeness | `ls docs/qa/2026-09-05` | `README.md`, `screenshots/` (61 route directories), `axe/` (96 JSON + keyboard + summaries), `lighthouse/` (24 reports + summary + diagnostics), `seo/`, `before-after/` (25 pages), `reports/` (incl. `e2e/` and `final/`), `pricing-matrix.md` (+ generator), `changed-files.md` (+ generator), `recheck/`; 1 722 WebP files, none above 150 KB (`find -name '*.webp' -size +150k` → 0) | this section |
| Links in this report | `node docs/qa/2026-09-05/reports/final/check-links.mjs . docs/16-release-report.md` | **72 unique file references, 0 missing** (exact paths, globs, numbered-document shorthands such as `docs/11`, and paths quoted relative to `apps/web`; counts in the output file) | `reports/final/check-links.txt` |
| Size of the evidence pack | `du -sb docs/qa/2026-09-05` | see `reports/final/check-links.txt` (last line) | — |

Not repeated here (unchanged since §7, no code affected by the D6 fix): integration and contract tests, the SEO gate (D5), Lighthouse (§6.3). Open after this run: D1 (social cards of the knowledge pages), D5, D7 and the owed evidence D10, D15, D16; D13 now also covers the regenerated baselines, the spec change and the QA-script fixes.

### 11.1 Follow-up run 2026-09-05 (task R2, after PERF / E1 / E2 / E3)

One closing run of the repository gates on the working tree described in the header table ("Follow-up run": HEAD `3da47f5` + the uncommitted follow-up changes, D22), then the production build served on port 3015 with `HOST_MARKETING=http://localhost:3015 HOST_APP=http://localhost:3015/app AI_DEV_FIXTURES=1` for the full Playwright suite. Scripts and outputs under `docs/qa/2026-09-05/followup/reports/` (`run-gates.sh`, `run-e2e.sh`, `_gates.log` with timestamps; `NO_COLOR=1 CI=1`, turbo `--force` so no cached log is replayed). Host: Windows 11, Node 24.18.0, pnpm 11.21.0.

| Gate | Command | Result | Output |
| --- | --- | --- | --- |
| Typecheck | `pnpm typecheck --force` | **exit 0**, 17 / 17 tasks, 0 cached | `followup/reports/typecheck.txt` |
| Lint | `pnpm lint --force` | **exit 0**, 17 / 17 tasks, 0 cached (incl. the new QA scripts `apps/web/scripts/qa/{bundle-analysis,island-check,sourcemap-attribution,living-core-*}.mjs`) | `followup/reports/lint.txt` |
| Unit tests | `pnpm test --force` | **exit 0**, 15 / 15 tasks, 0 cached; **823 tests passed**: catalog 20, core 35, events 8, policy 17, config 7, analytics 2, sdk 12, connectors 8, queue 4, collector 24, worker 16, ai 170, web 500 (58 files) | `followup/reports/test.txt` |
| Production build | `pnpm --filter @track-site/web build` | **exit 0**, compiled in 5.4 s, **715 / 715 static pages** in 10.4 s, `BUILD_ID` `L4YIgwPErJhRJVZXJ4S5X` (replaces `JkuZkqiqEn0HgN4FIDCyT` in `apps/web/.next`; the E3 fixes are now in the served build); the known `CONFIG_SIGNING_PUBLIC_KEY` warning (O6) | `followup/reports/build.txt` |
| Server | `HOST_MARKETING=http://localhost:3015 HOST_APP=http://localhost:3015/app AI_DEV_FIXTURES=1 pnpm --filter @track-site/web start -p 3015` | ready in 139 ms, `BUILD_ID` `L4YIgwPErJhRJVZXJ4S5X`; four `The destination stream closed early` lines from navigations aborted by the tests (the same message appears 16 × in `reports/final/server-3007.log` and in the PERF / E2 server logs — not new); stopped at the end, `Get-NetTCPConnection -LocalPort 3015` empty, no `next start` process left | `followup/reports/server-3015.log` |
| e2e, full suite (setup + chromium + visual), run 1 | `E2E_BASE_URL=http://localhost:3015 E2E_ENGINES= pnpm --filter @track-site/web test:e2e` | **exit 0, 55 / 55 passed** in 41.5 s: 1 setup sign-in, **42 chromium** (the 39 of §11 + the three E3 specs "mobile on-screen keyboard", "… survive a route change", "a site switch confirms the new context …" — all passed on the rebuilt server, so the keyboard fix is in the served build), **12 visual** against the committed `-win32` baselines — **no baseline was regenerated** (`git status apps/web/e2e/__screenshots__` clean): the perf placeholders / symbol icons, the contrast token changes and the wider pricing diagram node stay under `maxDiffPixelRatio` 0.01. `E2E_ENGINES=` (empty) leaves the optional `firefox` / `webkit` projects of E2 undefined: Firefox cannot start on this machine (D19) and WebKit fails 20 tests on the build-time CSP directive (D18); their results are the E2 matrix of §6.6 | `followup/reports/e2e-run1.log` |
| Visual regression, second consecutive pass | `… test:e2e --project=visual` (same server) | **13 / 13 passed** (setup + 12 snapshots) | `followup/reports/e2e-visual-run2.log` |
| Temporary specs | `ls apps/web/e2e/*.tmp.spec.ts` | none present (the E2 copies `*-xb.tmp.spec.ts` / `xbrowser-*.tmp.spec.ts` and the E3 probe were removed by their tasks; `apps/web/e2e` holds `README.md`, `__screenshots__/`, `app.spec.ts`, `auth-file.ts`, `auth.setup.ts`, `marketing.spec.ts`, `visual.spec.ts`) | this section |
| Other ports | `Get-NetTCPConnection` 3000–3100 before the run | no listener, no `next` process — the servers of PERF (3011), E1 (3012), E2 (3013) and E3 (3014) were stopped by their tasks | this section |
| Links in this report | `node docs/qa/2026-09-05/reports/final/check-links.mjs . docs/16-release-report.md` (also on docs/15 and docs/11) | **109 unique file references, 0 missing** (98 exact, 7 globs, 3 numbered-document shorthands, 1 relative to `apps/web`); docs/15: 14 references, 0 missing; docs/11: 23 references, 4 missing — all in its §1 "As-is" table naming pre-redesign files that the redesign removed (historical record, not touched here) | `followup/reports/check-links.txt` |
| Evidence pack completeness (follow-up) | `find docs/qa/2026-09-05/followup -name '*.webp' -size +150k` | **305 files, 32 WebP, 0 above 150 KB**, 103 075 632 B: `perf/` (Lighthouse 12 + 12 + 2 variant reports, bundle and attribution tables, HTML anatomy, island check), `living-core/` (15 Lighthouse reports, motion-off, WebGL probe, tier smoke), `browsers/` (engine probes, permanent-config logs, `runs/` with JSON records, WebP and diffs), `contrast/` (three results files ≈ 13.5 MB, summary of 1 935 evaluations), `keyboard/`, `state/`, `reports/` (this run) | `followup/reports/check-links.txt` (last lines) |

Not repeated here: integration and contract tests (no change to `packages/db`, `packages/queue`, the worker or the connectors in the follow-up; last run §7), the SEO gate and the crawl (the follow-up changed no route, redirect or metadata — the cache headers of C10 were verified with `curl -I` in `followup/perf/cache-headers-after.txt`), Lighthouse (the medians of §6.3.1 were measured on `JkuZkqiqEn0HgN4FIDCyT`; the gate build `L4YIgwPErJhRJVZXJ4S5X` adds only the E3 token / diagram / chat changes, not re-measured). Open after this run: D9 (reduced), D10 (long tasks, soak, zoom, visual comparison, Firefox / Edge / devices), D11, D17 (Living AI Core −6 points), D18–D21 (tooling / environment / spec findings of the matrix), D22 (uncommitted follow-up changes), D3, D4, D12, D14; fixed in the follow-up: D15, D16 (keyboard, route change, site switch).

### 11.2 Follow-up run 2 (2026-09-05, evening; task RP, after FX / MS)

One closing run of the repository gates on the working tree described in the header table ("Follow-up run 2": HEAD `caab018` + the uncommitted follow-up-2 changes, D23), then the production build served on port 3017 with `HOST_MARKETING=http://localhost:3017 HOST_APP=http://localhost:3017/app AI_DEV_FIXTURES=1` for the full Playwright suite. Scripts and outputs under `docs/qa/2026-09-05/followup2/reports/` (`README.md` indexes them; `run-gates.sh`, `run-e2e.sh`, `_gates.log` with timestamps; `NO_COLOR=1 CI=1`, turbo `--force` so no cached log is replayed). Host: Windows 11, Node 24.18.0, pnpm 11.21.0. The FX gates on the same source (`followup2/fx/{typecheck,eslint,vitest,build}.log`, exit 0 each, web 532 tests, build `9ZJAGzR3pMhmuGkmeGTk8`) and the MS measurements of §6.6 precede this run; between the FX build and this gate build only the QA scripts under `apps/web/scripts/qa/` changed (not part of the bundle).

| Gate | Command | Result | Output |
| --- | --- | --- | --- |
| Typecheck | `pnpm typecheck --force` | **exit 0**, 17 / 17 tasks, 0 cached | `followup2/reports/typecheck.txt` |
| Lint | `pnpm lint --force` | **exit 0**, 17 / 17 tasks, 0 cached (incl. the changed QA scripts `living-core-{budget,lighthouse,report}.mjs`, `next.config.ts` with its exported builders, the new `upgrade-gate.ts` and the inline-script layout) | `followup2/reports/lint.txt` |
| Unit tests | `pnpm test --force` | **exit 0**, 15 / 15 tasks, 0 cached; **855 tests passed** (was 823): catalog 20, core 35, events 8, policy 17, config 7, analytics 2, sdk 12, connectors 8, queue 4, collector 24, worker 16, ai 170, **web 532** (60 files; +32 for the FX suites `upgrade-gate.test.ts`, the `tier.test.ts` device-rule groups, the `living-ai-core.dom.test.tsx` gate / constrained-device groups and `src/next.config.test.ts`) | `followup2/reports/test.txt` |
| Production build | `pnpm --filter @track-site/web build` | **exit 0**, compiled in 4.5 s, **715 / 715 static pages** in 13.8 s, `BUILD_ID` `Oqin7qkrWeUgilSwQTrKM` (replaces `9ZJAGzR3pMhmuGkmeGTk8` in `apps/web/.next`); the known `CONFIG_SIGNING_PUBLIC_KEY` warning (O6) | `followup2/reports/build.txt` |
| Server | `HOST_MARKETING=http://localhost:3017 HOST_APP=http://localhost:3017/app AI_DEV_FIXTURES=1 pnpm --filter @track-site/web start -p 3017` | ready in 145 ms, `BUILD_ID` `Oqin7qkrWeUgilSwQTrKM` served (`/_next/static/Oqin7qkrWeUgilSwQTrKM/_buildManifest.js` → 200 during the run); seven `The destination stream closed early` lines from navigations aborted by the tests (the same message as in `reports/final/server-3007.log` and `followup/reports/server-3015.log` — not new); stopped at the end: `Get-NetTCPConnection -LocalPort 3017 -State Listen` empty, no `next start` / headless-shell process left | `followup2/reports/server-3017.log` |
| e2e, full suite (setup + chromium + visual), run 1 | `E2E_BASE_URL=http://localhost:3017 E2E_ENGINES= pnpm --filter @track-site/web test:e2e` | **exit 0, 55 / 55 passed** in 39.0 s: 1 setup sign-in (through the new `waitForLoginHydration`, D20), **42 chromium** (the 42 of §11.1 incl. the Living AI Core specs — motion `off` → `data-tier="static"`, reduced motion → static from SSR — and the E3 keyboard / state specs), **12 visual** against the committed `-win32` baselines — **no baseline was regenerated** (`git status --short apps/web/e2e/__screenshots__` empty): the `visual` project runs with `reducedMotion: "reduce"` (`playwright.config.ts`, repeated by `page.emulateMedia` in `visual.spec.ts`), so the core renders the `static` tier in the `app-overview` snapshots — a path the FX change does not touch (the gate is never armed under reduced motion) — and the SSR / first paint of every other page is unchanged; the static-tier first paint therefore did not change and the one-time regeneration allowed for that case was not needed. `E2E_ENGINES=` (empty) leaves the optional `firefox` / `webkit` projects undefined: Firefox cannot start here (D19); the WebKit re-run without the header-strip hook that would close D18 / D20 / D21 was not part of this task | `followup2/reports/e2e-run1.log` |
| Visual regression, second consecutive pass | `… test:e2e --project=visual` (same server) | **13 / 13 passed** (setup + 12 snapshots) in 29.8 s | `followup2/reports/e2e-visual-run2.log` |
| Temporary specs | `ls apps/web/e2e/*.tmp.spec.ts` | none present (`apps/web/e2e` holds `README.md`, `__screenshots__/`, `app.spec.ts`, `auth-file.ts`, `auth.setup.ts`, `marketing.spec.ts`, `visual.spec.ts`) | this section |
| Other ports | `Get-NetTCPConnection` / `Win32_Process` before the run | no listener on 3017, no `next start` or headless-shell process — the servers of FX (none) and MS (3016) were stopped by their tasks | this section |
| Links in this report | `node docs/qa/2026-09-05/reports/final/check-links.mjs . docs/16-release-report.md` (also on docs/15 and docs/11) | **130 unique file references, 0 missing** (118 exact, 8 globs, 3 numbered-document shorthands, 1 relative to `apps/web`); docs/15: 20 references, 0 missing; docs/11: 24 references, 4 missing — the same 4 historical "as-is" references to files the redesign removed as in §11.1 (not touched) | `followup2/reports/check-links.txt` |
| Evidence pack completeness (follow-up 2) | `find docs/qa/2026-09-05/followup2 -type f` | **59 files, 10 854 587 B (at the time of the check; the `reports/README.md` inside the pack was edited once more afterwards), 0 WebP**: `fx/` (9 files: gate logs, CSP / viewport build checks, jsdom probes, `README.md`), `living-core/` (Lighthouse 9 × JSON + 9 × HTML, `tier-checks.json`, `lcp-elements.json`, `summary.json`, `summary.md`, `run.log`; long-task JSON + trace summary + logs; soak JSON + CSV + logs; motion JSON + log; server log; generated `summary.md`, `summary-notes.md`, `README.md`), `reports/` (this run: scripts, gate outputs, server and e2e logs, `README.md`); the 341 MB Chrome trace of the long-task run is deliberately outside the pack (path in `longtasks-d3d11.json`) | `followup2/reports/check-links.txt` (last lines) |

Not repeated here: integration and contract tests (no change to `packages/db`, `packages/queue`, the worker or the connectors in follow-up 2; last run §7), the SEO gate and the crawl (no route, redirect or metadata change — the dashboard viewport meta lost the `interactive-widget` key, which the checker does not evaluate), Lighthouse of the marketing pages (unchanged since §6.3.1). Open after this run: D9 (reduced), D10 (environment-bound items only: real devices, Firefox / Edge, 200 % zoom, hidden tab in a browser, visual comparison, INP), D11, D18 / D20 / D21 (fixed in code, WebKit re-run owed), D19, D23 (uncommitted follow-up-2 changes), D3, D4, D12, D14; fixed in follow-up 2: D17 (measured −1 point), the measurable part of D10 (long tasks, soak, reduced motion), D22 (committed as `caab018`).
