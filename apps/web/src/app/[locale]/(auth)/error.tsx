"use client";

/**
 * Error boundary of the (auth) group: the same localized error view as the marketing pages, but
 * rendered inside the compact auth frame instead of the marketing chrome.
 */
export { default } from "../(marketing)/error";
