/**
 * Dashboard shell entry point. The implementation lives in `./shell/*` (viewport-fixed layout,
 * navigation, workspace switcher, environment indicator, command palette, Track AI host).
 */
export { AppShell } from "./shell/app-shell";
export type { ShellOrganization, ShellProps, ShellUser, ShellWorkspace } from "./shell/types";
