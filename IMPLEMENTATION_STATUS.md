# IMPLEMENTATION_STATUS

States: `DONE` (implemented + tested), `PARTIAL` (usable, incomplete; see note), `BLOCKED` (needs external action), `NEXT` (next executable step). Updated 2026-09-05 (release report `docs/16-release-report.md`, evidence pack `docs/qa/2026-09-05/`).

| Area | State | Evidence | Test command | Note |
| --- | --- | --- | --- | --- |
| Target repo audit + docs 00-10 + ADRs 0001-0008 | DONE | `docs/` | - | target `soheilxx/track.site`, branch `feat/ai-tag-manager-platform` pushed 2026-09-03, `main` created from it the same day (Vercel production branch), CI runs on push |
| Monorepo foundation (pnpm 11, turbo, TS 5.9, eslint flat, prettier) | DONE | root configs | `pnpm lint && pnpm typecheck` | apps/web lint pins React version for eslint-plugin-react |
| packages/core (env, logger, ids, crypto, PII, URL scrub, RBAC, result) | DONE | `packages/core` | `pnpm --filter @track-site/core test` | 32 tests |
| packages/db schema + migrations 0000-0013 + RLS + partitions | DONE | `packages/db` (`drizzle/*.sql` hand-written and idempotent, journal `drizzle/meta/_journal.json`, `src/cli/schema-check.ts`) | `pnpm test:integration`, `pnpm --filter @track-site/db migrate:check`, `pnpm --filter @track-site/db schema:check` (against a `*_test` database migrated from zero) | 0002 credential kinds, 0003 `shop_connections`, 0004 tariff catalogue, 0005 knowledge feedback, 0006 workspace preferences, 0007 event lineage + test lab runs, 0008 destination health snapshots, 0009 inbox workflow + revenue reconciliation, 0010 release approvals + scheduled publish, 0012 approval policy + approval requests (0011 unused), 0013 alert channels/rules/events; the journal-driven run on a fresh database is object-for-object identical to the hand-applied dev database (1 934 columns/indexes/constraints/policies/enums compared, 0 differences); `schema:check` = drizzle schema ↔ database plus RLS + policy on every tenant table |
| Auth (better-auth, orgs, roles, invites, MFA, passkeys) | DONE | `apps/web/src/server/auth.ts` | browser walkthrough | signup → verification mail → org → site verified in browser |
| Site + 6-char tracking id + domain verification | DONE | `packages/core/src/ids`, `packages/db/src/repositories/sites.ts` | `pnpm --filter @track-site/core test` | site TI8R42 created in browser |
| SDK tracker.js + consent gate + signed config + vendor loaders (22 templates) + first-party click-id store | DONE | `packages/sdk` | `pnpm --filter @track-site/sdk test && pnpm --filter @track-site/sdk budget` | 36.7 KB raw / ~12 KB gzip of 30 KB budget; `_ts_cid` under marketing consent with bundle TTL; purchases mirror with `purchase:<order id>` |
| Collector + PgQueue + worker pipeline + event store + DLQ/replay | DONE | `apps/collector`, `apps/worker`, `packages/queue`, `packages/analytics` | `pnpm --filter @track-site/collector test`, `pnpm --filter @track-site/worker test:integration` | inbound affiliate postbacks (Digistore24 IPN); browser↔server order pairing (consent/click-id inheritance, verified record supersedes); shared vendor dedup id `vendorDedupId`; retention job honours org overrides |
| Config bundles (draft → lint → publish → rollback, Ed25519 signed) | DONE | `packages/config`, `packages/db/src/repositories/config.ts` | `pnpm --filter @track-site/config test` | version 1 published from the wizard in browser |
| Consent policy engine + Consent Mode v2 + click-id policy | DONE | `packages/policy` | `pnpm --filter @track-site/policy test` | 22 destination types in the matrix |
| AI setup (Responses API, typed tools, structured UI, approvals, DLP) | DONE | `packages/ai`, `apps/web/src/app/api/ai/*` | `pnpm --filter @track-site/ai test` (74 tests) | live-tested with production key 2026-09-03; tool-contract audit (50 confirmed findings) implemented, see docs/08 |
| Destination wizard (19 steps) + catalog + monitoring + OAuth connect | DONE | `apps/web/src/components/destinations/*`, `apps/web/src/app/app/destinations`, `apps/web/src/server/oauth.ts` | browser walkthrough | OAuth providers need platform client ids |
| Connectors — Group 1 (Meta, Google Ads, TikTok, Microsoft, LinkedIn, Reddit, Pinterest, Snapchat) + GA4 + webhook | DONE | `packages/connectors/src/vendors/*` | `pnpm --filter @track-site/connectors test:contract` | 61 contract tests against mock vendors |
| Connectors — Group 2 (X, Taboola, Outbrain, Amazon, Spotify, Quora) | DONE | same | same | Amazon/Quora bodies verified through secondary references (see docs/09) |
| Connectors — Group 3 (Yahoo, Trade Desk, GMP/Floodlight, AdRoll, Criteo, affiliate S2S with 13 presets) | DONE | same, `affiliate-presets.ts` | same | AdRoll beta + TTD tag prerequisites shown in the wizard |
| Integration matrix (auto-checked) + handover | DONE | `docs/integrations-matrix.md`, `docs/09-integrations-handover.md` | `pnpm --filter @track-site/connectors test` | regenerate with `pnpm --filter @track-site/connectors matrix` |
| Event debugger + lineage UI (now the Live Event Explorer, `/app/events/explorer`; `/app/debugger` answers 308) | DONE | `apps/web/src/app/app/events/explorer/page.tsx`, `apps/web/src/server/events-lineage.ts`, `apps/worker/src/stages/lineage.ts`, destination monitor, `get_destination_status`, `show_delivery_errors` | browser walkthrough | redacted payload preview, consent record, per-stage lineage (`event_lineage`) and policy decision per destination |
| Dashboard redesign (supplement §8/§9): viewport-fixed shell with navigation, workspace switcher, environment indicator, Ctrl/Cmd+K palette (navigation, sub-modules, sites, destinations, actions) and Track AI panel; Command Center; AI Setup; Events (overview, Event Coverage Matrix, Live Event Explorer with lineage, Live Test Lab); Destination Health Center; Data Quality Inbox + Signal Gap & Revenue Leak Detector; Consent & Privacy + Consent Impact Simulator; Insights (Attribution & Click-ID Health, audiences); Change & Release Center + Change Impact Preview + scheduled publish + four-eyes approvals; Team & Access + approval policy + audit log; Billing + Usage & Cost Guard; Alerts & Incident Mode (`/app/settings/alerts`) + settings sub-navigation + per-user AI motion preference | DONE | `apps/web/src/app/app/**`, `apps/web/src/components/app/{shell,command-center,events,destinations-health,data-quality,consent,insights,releases,team,billing}/`, `apps/web/src/server/{command-center,events,events-lineage,destination-health,data-quality,revenue-leaks,consent,consent-simulator,insights,releases,team,usage,workspace,alerts,preferences}.ts` + `actions/*`, `apps/web/src/components/app/{alerts,settings}/`, catalogs `apps/web/messages/<locale>/{shell,command-center,events,destinations-health,data-quality,consent,insights,releases,team,billing-usage,alerts}.json` ×6 (registry `apps/web/src/i18n/namespaces.ts`), 308s `/app/setup` → `/app/ai-setup`, `/app/debugger` → `/app/events/explorer`, `/app/audiences` → `/app/insights/audiences` (`apps/web/next.config.ts`) | `pnpm --filter @track-site/web test` (incl. `i18n/namespaces.test.ts` parity ×6 and the module unit tests), `pnpm --filter @track-site/web i18n:parity --strict`, `pnpm --filter @track-site/web typecheck`, `pnpm --filter @track-site/web lint`, `pnpm --filter @track-site/web build`, browser walkthrough with the seeded demo org | Alerts & Incident Mode (module 13) built 2026-09-04: channels (e-mail in clear, webhook/Slack URLs and the webhook signing secret envelope-encrypted, host hint only, test notification), rules (six kinds, per-kind thresholds with the bounds of `ALERT_THRESHOLD_FIELDS`, plain-language preview, cooldown), filtered history with manual resolve, Incident Mode (destination pause/resume reusing the health center path; environment kill switch = derived signed version with `settings.kill_switch` via `publishDerivedVersion`; four-eyes policy respected with `approval_required`), every mutation audited; the environment kill switch stops the browser tracker only (collector/worker do not read `bundle.settings.kill_switch`, server-side events continue — the UI says so). Settings locale enum accepts all six locales (`z.enum(ALL_LOCALES)` in `server/actions/settings.ts`; the unused legacy `updateLocaleAction("en" \| "de")` signature is cleanup item D12 in docs/16); `apps/web/e2e/app.spec.ts` (the three 308s with query string, one `h1` inside the shell on every module route incl. `/app/settings/alerts`, alerts page with channel + rule round trip, motion preference, viewport-fixed shell, palette, mobile bottom sheet, 250-message Track AI panel) ran green on 2026-09-05: 28 passed twice (`docs/qa/2026-09-05/reports/e2e/e2e-run{2,4}.log`; one flaky reduced-motion run, docs/16 D11) plus the 12 responsive/focus specs added by the QA fixes (`docs/qa/2026-09-05/recheck/e2e-new-specs.log`); the shell overflow at 320–768 px and the missing focus ring on the switcher/account menu found by the QA sweep are fixed in the (uncommitted) F1 working tree (docs/16 §6.1–6.2, D13); open defects: two `h1` on `/app/destinations` and an unprefixed `/contact` link on `/app/billing` (docs/16 D2); pages fed by worker jobs (destination health, data-quality inbox, revenue leaks, change-impact volumes) show honest "not measured" states until the jobs have run; `apps/web/src/server/insights-legacy.ts` (pure helper of the retired audiences shim) still exists because `insights-actions.ts` imports its path constant |
| Track AI (supplement §9, redesign phase 6): allow-listed UI stream (`packages/ai/src/ui-events.ts`, chat + confirm routes, resumable turns), localized activity sentences ×6, scope gate with injection evals, Living AI Core (static/CSS/WebGL2 tiers, state machine, per-user motion setting + header pause), panel wiring: activity feed per tool run with next actions, windowed message list from 200 messages with stable scroll anchoring, "Show new messages" pill, off-topic notice with ≤ 3 quick actions, approval reveal/focus, credential-card entry points, announcer, deterministic workspace moves for turns started on the setup page, first-run stage on `/app/ai-setup` docking into the panel (one animated core at a time), card outcomes as localized system notes (no synthetic user messages), dev-only fixture route | DONE | `packages/ai/src/{ui-events,scope,scope-copy,turn-registry}.ts`, `apps/web/src/components/app/shell/{assistant-host,assistant-panel}.tsx`, `apps/web/src/components/app/shell/living-ai-core/**`, `apps/web/src/components/chat/{assistant-chat,assistant-store,chat-reducer,workspace-moves,use-workspace-moves,virtual-list,next-action,focus-target,setup-chat,wizard,inputs}.tsx?`, `apps/web/src/app/app/ai-setup/page.tsx`, `apps/web/src/app/api/ai/dev-fixture/`, `apps/web/messages/<locale>/{assistant,shell}.json` ×6, docs/08, docs/15 | `pnpm --filter @track-site/ai test`, `pnpm --filter @track-site/web test` (58 files / 497 tests incl. `assistant-chat.test.tsx`, `workspace-moves.test.ts`, `virtual-list.test.ts`, `next-action.test.ts`, `dev-fixture/*.test.ts`), `pnpm --filter @track-site/web typecheck`, `pnpm --filter @track-site/web lint`, `pnpm --filter @track-site/web i18n:parity -- --strict`, `pnpm --filter @track-site/web build`, `pnpm --filter @track-site/web test:e2e` ("Track AI panel": 250 messages keep the shell viewport-high with a windowed list, motion `off` → `data-tier="static"` and no running core animations, `prefers-reduced-motion` → static tier from SSR on, activity live region + setup targets) | Workspace moves run only in a setup context (first run = no published configuration, the mounted setup workspace, or a turn started there) so no conversation hijacks another module's page; the first-run stage needs a site without an active version (the seeded demo site is published, so the e2e covers the docked workspace only); the fixture route answers 404 when `NODE_ENV` or `APP_ENV` is `production` and the store then loads the real transcript; the e2e specs were written against the code and still need a run against a dev server with the seeded demo organization (`pnpm --filter @track-site/web test:e2e`) — no server in this agent run; Lighthouse/long-task/soak evidence for the core is owed by phase 7 (docs/15 §4) |
| Track redesign programme (supplement of 2026-09-03, phases 1–7): brand `Track` + mark, design system (`packages/ui` tokens/primitives), home with interactive demo, catalogue-driven pricing (Starter/Growth/Pro/Enterprise, overage packs, trial, plan finder, calculator), Blog → Tracking Knowledge (hub, article template, social cards, JSON-LD, redirect matrix), six locales with parity, Tracking Command Center + Track AI, QA evidence pack and release report | PARTIAL | `docs/11-track-redesign-program.md` (phases, decisions, owner actions), `docs/16-release-report.md` (Abschlussbelege 1–8, defects with owner), `docs/qa/2026-09-05/` (before/after 25 pages × 4 widths, responsive sweep 336 + 144 runs, axe, keyboard, Lighthouse 24 + 8 runs, crawl of 498 pages, six-locale redirect matrix, pricing matrix, gate outputs, e2e/visual logs) | `pnpm typecheck && pnpm test && pnpm test:contract && pnpm test:integration --concurrency=1 && pnpm --filter @track-site/web build` (all green 2026-09-05, `docs/qa/2026-09-05/reports/*.txt`: 823 unit, 63 contract, 22 integration tests, 715/715 pages); `pnpm --filter @track-site/web knowledge:validate`, `node apps/web/scripts/i18n-parity.mjs --strict`; `pnpm --filter @track-site/web test:e2e` (28 passed + 12 new specs, logs in the pack); `pnpm lint` currently fails only in the untracked `apps/web/scripts/qa/*.mjs`; `pnpm seo:check` fails on stale checker rules (replace by `node apps/web/scripts/qa/crawl.mjs`) | PARTIAL, not DONE, because (docs/16 §9): D1 `og:image`/`twitter:image` of the knowledge hub and all 180 articles answer 404 (page metadata composes `…/opengraph-image` while Next.js emits the dynamic metadata route as `opengraph-image-<hash>`); D9 mobile performance 87–94 / LCP 2.9–4.0 s below the ≈95 / ≤2.5 s targets (a11y 100, BP 96–100, SEO 100, CLS ≤ 0.03 pass; desktop 99); D13 the F1 fixes (35 files) and the QA tooling are uncommitted; gate tooling D5–D7 (`seo-check.ts` stale, eslint in QA scripts, integration global-setup deadlock under parallel turbo); owed evidence D10 (Living AI Core budget of docs/15 §4, cross-browser), D16 (mobile keyboard vs composer, route-change state) |
| Worker scheduled jobs: outbox 5 s, usage 60 s, partitions 6 h, retention 24 h, destination-health 60 s, data-quality (reconciliation + inbox scan) 1 h, scheduled-publish 30 s, alerts 60 s (rule evaluation from aggregates, health snapshots and credentials; e-mail / HMAC-signed webhook / Slack delivery with per-channel outcome); a tick is skipped while the previous run of the same job is still going; `/health` lists every job with interval, run count, last run and last error | DONE | `apps/worker/src/jobs/index.ts` (`JOB_SCHEDULE`), `apps/worker/src/jobs/{destination-health,reconciliation,scheduled-publish,alerts,alerts-text,alerts-mail}.ts`, `apps/worker/src/main.ts`, docs/07 §Worker jobs | `pnpm --filter @track-site/worker typecheck && pnpm --filter @track-site/worker test` (16 fixture tests for the alert rule evaluation, cooldown and localized text) | `alerts` needs the worker vault (`MASTER_KEY`) for webhook/Slack URLs and `SMTP_URL`/`RESEND_API_KEY` for e-mail (nodemailer added to the worker); scheduled publish needs `CONFIG_SIGNING_PRIVATE_KEY` + `CONFIG_SIGNING_KEY_ID` on the worker (declared in `apps/worker/src/env.ts`; without them a due draft is refused with `schedule_error = signing_key_missing`); the other jobs have no unit tests, only the worker pipeline integration test |
| Commerce integrations Shopify / WooCommerce / Shopware 6 | DONE | `apps/collector/src/shop-inbound.ts`, `packages/db/src/repositories/commerce.ts`, `apps/web/src/app/app/sites/[siteId]/shop`, `integrations/*`, `docs/10-commerce-integrations.md` | `pnpm --filter @track-site/collector test`, `pnpm --filter @track-site/policy test`, `pnpm --filter @track-site/worker test:integration` | signed webhooks verified (HMAC), Shopify web pixel, WooCommerce plugin with managed webhooks, Shopware app manifest + registration handshake; local E2E: signed `orders/paid` → verified purchase + conversion record + connection `connected` |
| Marketing frontend, SEO, Tracking Knowledge (30 topics x en/de) | DONE | `apps/web/src/app/[locale]/*`, `apps/web/content/knowledge/{en,de}` (30 + 30 articles), `apps/web/scripts/seo-check.ts` | `pnpm seo:check` (against a running web server) | typed copy per locale, legal identity from `LEGAL_*` env, prices only from Stripe, MDX articles with front matter, sources and review dates |
| Blog → Tracking Knowledge (routes, 301 matrix, taxonomy, social cards, JSON-LD) | DONE | `apps/web/content/knowledge/{en,de}`, `apps/web/src/lib/knowledge.ts`, `apps/web/src/app/[locale]/tracking-knowledge/*` (index, `[slug]`, `feed.xml`, `opengraph-image`), `apps/web/next.config.ts` (permanent redirects, no chains), `docs/redirects-blog-to-tracking-knowledge.md`, scripts `migrate-knowledge-frontmatter.mjs`, `rebrand-knowledge-content.mjs`, `redirect-matrix.mjs`, `apps/web/src/components/marketing/localized-paths.ts` | `pnpm --filter @track-site/web test` (`knowledge.test.ts`, `seo.test.ts`, `localized-paths.test.ts`), `node apps/web/scripts/redirect-matrix.mjs`, `node apps/web/scripts/migrate-knowledge-frontmatter.mjs --check`, `node apps/web/scripts/rebrand-knowledge-content.mjs --check` | `translationGroupId` + per-locale `slug`, topics/platforms/shop systems/content type/level, statuses `draft/translated/reviewed/published` (only `published` listed and indexed), `BreadcrumbList` + `BlogPosting` JSON-LD with publisher logo `/brand/icon-512.png`, generated 1200×630 cards, brand "Track" in titles/`og:site_name`/structured data/article text, language switcher follows the article's localized alternates; hub redesign (search, learning paths, article template) is phase 3 |
| Localization ×6 (en/de/fr/es/it/nl: UI message catalogs, marketing/legal/mail copy, knowledge labels, integration catalogue text, 30 Tracking Knowledge articles + learning paths per locale, 95 tariff labels; enable stage) | DONE | `apps/web/src/i18n/routing.ts` (`ACTIVE_LOCALES` = six), `apps/web/src/lib/marketing-copy/types.ts` (`COPY_LOCALES` = six), `apps/web/src/proxy.ts` (no inactive-locale redirect), `apps/web/messages/{fr,es,it,nl}`, `apps/web/src/lib/marketing-copy/<area>/{fr,es,it,nl}.ts` incl. new area `integration-catalog/` (+ `components/marketing/integrations/text.ts`), `apps/web/src/lib/legal-copy/`, `apps/web/src/server/mail/templates/`, `apps/web/content/knowledge/{fr,es,it,nl}` + `paths.<locale>.json`, `apps/web/src/lib/format.ts` (Intl per locale, used by pricing/billing/knowledge), `apps/web/scripts/i18n-parity.mjs`, `docs/i18n-parity-report.{md,json}`, `docs/14-localization.md` | `pnpm --filter @track-site/web test` (229 tests: `i18n/routing.test.ts` six active locales + catalogs + 30 published articles + learning paths per locale, `pick.test.ts` every copy module ×6, `proxy.test.ts`, `catalog.test.ts` integration text ×6), `node apps/web/scripts/i18n-parity.mjs --strict` (exit 0, zero gaps in every section), `pnpm --filter @track-site/web knowledge:validate` (180 files, 30 groups, six locales), `pnpm --filter @track-site/web build` (715 prerendered pages, ≈117 per locale — roughly triple the two-locale output), `pnpm --filter @track-site/web test:e2e` (home ×6: `lang`, one `h1`, 7 hreflang links incl. `x-default`, axe; switcher en → de → fr stays on /pricing) and `pnpm seo:check` against a running server | hreflang ×6 + `x-default` and OG locale set on every page via `pageMetadata`, sitemap index with `pages-<locale>.xml` + `knowledge-<locale>.xml` ×6, RSS feed and localized 404 per locale, robots unchanged, `NEXT_LOCALE` cookie unchanged (deliberate choice only, no geo detection), localized slugs identical to English for the release; contact/demo/support action accepts every programme locale and `REQUIRED_LABEL_LOCALES` (`packages/catalog`) lists all six; open (dashboard scope, phase 5): zod locale enum in `server/actions/settings.ts` and the language select in `components/app/settings.tsx` still list en/de |
| Stripe billing + entitlements + usage ledger | DONE | `apps/web/src/server/billing.ts`, `pricing.ts`, `apps/web/src/app/api/stripe/webhook/route.ts`, `/app/billing` | `/api/health` → `billing: ok` with all six prices validated (active, recurring, interval, tax behaviour); www.track.site/pricing shows the live amounts; unsigned webhook call → 400 SIGNATURE_INVALID | LIVE on the shared company Stripe account with a restricted key and a dedicated webhook destination; `STRIPE_PRICE_*` accepts price or product ids; end-to-end test purchase pending |
| Tariff catalogue (`@track-site/catalog`): plans Starter/Growth/Pro/Enterprise, list prices 19/90/180 € and 190/900/1 800 €, entitlements, feature gates, overage packs + policies (`allow`/`cost_limit`/`pause`, default pause, 70/90/100 % warnings), 14-day Growth trial definition, billable-event rules, plan finder, cost calculator | DONE | `packages/catalog`, migration `packages/db/drizzle/0004_catalog_pro_plan.sql` (`scale` → `pro`, `warned_70/90_at`, `organization_settings.usage_overage_policy/usage_cost_limit_cents`), seed syncs `plans` from the catalogue, `apps/web/src/server/{pricing,billing,entitlements}.ts`, `/api/health` `amount_mismatch` + deprecated `SCALE` names, `apps/worker/src/jobs/usage.ts` (70/90/100, policy-driven hard limit), ingest ledger excludes test-mode/duplicate/retry events | `pnpm --filter @track-site/catalog test` (19 tests) | pricing page renders catalogue prices (no Stripe dependency); owner must recreate the yearly Stripe prices (docs/11 §6); trial flow, plan finder UI, calculator UI and the usage guard page belong to the redesign phases |
| Privacy center, DSAR, retention | DONE | `/app/consent`, `apps/web/src/server/actions/privacy.ts`, `apps/web/src/app/api/privacy/dsar/[id]/route.ts`, `apps/worker/src/jobs/retention.ts` | browser walkthrough | export/delete/restrict/rectify with audit + completion report; retention overrides per org (site overrides for events/click ids) |
| CI gates | DONE | `.github/workflows/ci.yml` | first run on push | lint, typecheck, unit, contract, integration (Postgres 18 service), SDK budget, matrix check, production build; all gates pass locally (`pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm test:integration`, `pnpm --filter @track-site/web build`: 173 static pages) |
| Playwright smoke tests (marketing a11y/SEO structure, blog, pricing honesty, dashboard sign-in, shop connection page) | DONE | `apps/web/e2e/*.spec.ts`, `apps/web/playwright.config.ts` | `pnpm --filter @track-site/web test:e2e` (against a running server, seeded demo user) | axe-core WCAG 2 A/AA: contrast tokens raised to AA |
| SEO gate | DONE | `apps/web/scripts/seo-check.ts` | `pnpm seo:check` | 113 pages (en/de static, integrations, 60 blog posts), robots, sitemap, feed; titles/descriptions cut for snippets |
| Load test baseline | DONE | `docs/performance-baseline.md` | `pnpm load:collector` | dev laptop: 483 req/s = 2,417 events/s, p95 189 ms, 0 errors; limiter runs show 429 at the configured per-IP/per-site limits; single worker drained 70k events within 2.5 min |
| Data plane on Fly.io (collector + worker, Frankfurt) | DONE | `infra/docker/*.Dockerfile`, `infra/fly/*.toml`, docs/07 §Data plane | `fly status -a track-site-collector`, collector `/health` | live at https://track-site-collector.fly.dev (db ok, queue ok); worker running with retention job; ingest host https://ingest.track.site live with certificate |
| Vercel deployment of apps/web | DONE | `apps/web/vercel.json`, docs/07 §Vercel | `SEO_BASE_URL=https://www.track.site pnpm seo:check` | live at https://www.track.site (project modernice/track-site, fra1); dashboard needs `DATABASE_URL`; collector/worker still need an EU container host |

## External blockers (owner action)

| Blocker | Needed for | Action |
| --- | --- | --- |
| OpenAI API key per environment (projects exist) | live AI assistant | set on production (`https://www.track.site/api/health` → `ai: ok`, 3 models available, 2026-09-05); still to set per additional environment (`OPENAI_API_KEY`, List models: read, Responses: write) |
| Fly worker secrets `RESEND_API_KEY` (or `SMTP_URL`), `MAIL_FROM`, `HOST_APP` and the worker `MASTER_KEY` / KMS | Alerts & Incident Mode delivery (e-mail, encrypted webhook/Slack URLs); today alert events record `delivery[channel].error` | set the Fly secrets on `track-site-worker` (docs/07 §Worker jobs) |
| Stripe end-to-end test purchase on the live account | billing loop sign-off (checkout → webhook → entitlement → portal); the six prices verify (`billing: ok`) and unsigned webhooks are rejected | owner runs a checkout with a test card/100 % coupon and checks `/app/billing` |
| Legal approval of the imprint, privacy, terms, data-processing and subprocessor texts ×6 and of the operator identity (`LEGAL_*`) | binding legal pages (the live imprint already renders an operator identity) | owner / counsel |
| `CONFIG_SIGNING_PUBLIC_KEY` in the Vercel build environment | production tracker that verifies signed configurations (local builds embed no key and fail closed) | check the Vercel env (docs/07 §Vercel) |
| Vendor platform apps: Google Ads developer token + OAuth client, LinkedIn app, Login with Amazon app, X developer app | OAuth connectors | register apps, set `GOOGLE_ADS_DEVELOPER_TOKEN`, `GOOGLE_OAUTH_*`, `LINKEDIN_*`, `AMAZON_ADS_*`, `X_CONSUMER_*` |
| Vendor accounts for production sends | live vendor verification | first real test events per destination (docs/09 §7) |
| DNS for `track.site`, `app.`, `api.`, `cdn.`, `ingest.` | production hosts | configure DNS + TLS |
| EU infrastructure (DB, SQS, S3, KMS, ClickHouse) | production data plane | provision via `infra/terraform` |

## Next executable step

Release close-out from `docs/16-release-report.md` §9, in this order: (1) fix D1 — let the Next.js file convention emit the `og:image` URLs of the knowledge hub and articles (or read the generated hashed URL) and re-run `node apps/web/scripts/qa/crawl.mjs` until 0 broken image targets; (2) resolve the gate tooling D5–D7 (`apps/web/scripts/seo-check.ts`: count only the document `<title>`, accept `TechArticle`; eslint errors in `apps/web/scripts/qa/*.mjs`; serialize `packages/db/src/testing/global-setup.ts` so `pnpm test:integration` passes under turbo's default concurrency) and commit the F1 fixes + QA tooling on the feature branch (D13); (3) fix D2a/D2b/D3/D4/D12; (4) performance work for D9 (less hydration JS on marketing pages, single responsive comparison matrix, smaller HTML) and re-measure with `apps/web/scripts/qa/lighthouse.mjs --runs 3`; (5) produce the owed evidence D10 and D16 (Living AI Core budget of docs/15 §4, cross-browser matrix, mobile keyboard vs composer, route-change state) and add a Linux Playwright job to CI (D14). Owner actions from the blocker table (vendor OAuth apps, first organisation and site, Stripe test purchase), then the first real test event per destination (docs/09 §7) and the shop integrations against one real Shopify, WooCommerce and Shopware installation each (docs/10 checklist). Engineering follow-ups: ClickHouse event-store integration test on real ClickHouse, SQS queue driver soak, production load test on EU infrastructure against the targets in `docs/performance-baseline.md`.
